# Memória do projeto

> Fatos DURÁVEIS destilados automaticamente do fluxo (decisões, gotchas, invariantes), ancorados por
> ENTIDADE. **Gerado pelo H1VE — não editar à mão** (é sobrescrito). Eterno: nada é apagado.

## 🧭 Invariantes (valem sempre — leia antes de mexer na área)

- **Guardrail ESLint barra Intl.DateTimeFormat e Date em packages/*/src; único Date.now permitido é no harness** — eslint.config.mjs estende o guardrail para proibir Intl.DateTimeFormat (além de Date já barrado) dentro de packages/*/src. O único ponto de impureza de relógio/fuso permitido é harness/run-season.ts. Violar isso quebra o determinismo cross-plataforma silenciosamente. · `world-engine` `guardrail` `eslint`
- **Orçamentos do cliente medidos contra o process tree inteiro** — <1% CPU (gate de CI, governante) e <150 MB RAM são constraints duros do cliente Windows. O budget de RAM deve ser verificado contra o process tree inteiro — não só o processo principal. Isso é decisivo para qualquer candidato baseado em WebView2 (Tauri, etc.), cujo processo msedgewebview2 soma ao total. 'Web-wrapper ultraleve' só conta como candidato se usar WebView2 do sistema (não Chromium empacotado) E passar o gate <1% CPU sob build real medido. · `cliente` `rate-limit` `stack`
- **Ordem canônica da viragem de temporada é imutável** — A ordem das etapas em `advanceWorld` é: promoção/rebaixamento → envelhecer → aposentar → transferir → repor base → recomputar força → seasonId++. Alterar essa ordem muda o stream do PRNG e quebra o golden vector. Documentada no código e testada por golden. · `world-engine` `world-turnover` `prng`
- **Tunáveis canônicos do world-engine: rosterSize=16, squadShape={GK:2,DEF:5,MID:5,FWD:4}, transfersPerLeague=10** — Valores fixados na SPEC-012 (R4 final). Qualquer alteração nesses três campos em constants.ts muda o número de saques do PRNG e invalida world.golden.json — exige regen intencional via harness/regen-world-golden.ts. strengthTopN permanece 11 (16≥11 mantém clubStrength válida). · `world-engine` `constants` `determinism`
- **Snapshot é cache; seed é fonte-da-verdade; strength nunca persistida** — O snapshot Postgres é cache consultável, reconstruível por replay — nunca autoridade. A `seed` (coluna de 1ª classe na tabela `world`) é a fonte-da-verdade. `strength` do clube NÃO é persistida: é derivada por `clubStrength(roster)` na leitura. Qualquer schema futuro deve manter essas três regras para não criar duas fontes de verdade. · `world-store` `schema` `world-engine`
- **await onBeforeCommit obrigatório: sem await, rejeição async commita errado** — O seam onBeforeCommit DEVE ser awaited dentro do db.transaction. Sem await, uma rejeição de Promise vaza como unhandledRejection e a transação commita mesmo em falha — regressão documentada desde a SPEC-002. Qualquer futura edição de publishRound deve preservar o await onBeforeCommit?.(). · `world-store` `round-repo` `published_round`
- **Snapshot do mundo deve ser imutável dentro da temporada (re-simulação a cada tick)** — runDailyRound re-simula simulateWorldSeason a cada tick e fatia rounds[N-1]. Isso só é seguro enquanto o snapshot for imutável na temporada (v1 = 100% NPC congelado). Substituir NPC por humano tornaria o snapshot mutável e quebraria a re-simulação — é gate para fatia futura que deve congelar o plano da temporada no início. · `daily-round` `world-engine` `simulation`
- **player-store usa dois schemas Postgres separados: `player` (tabelas) e `drizzle_player` (tracking de migrations)** — O world-store já possui `public.athlete`, então as tabelas do player vivem no schema `player`. O tracking de migrations NÃO pode usar o mesmo schema `player` porque o migrator precisa criar o schema de tracking ANTES de rodar a migration — e a migration `0000` já faz `CREATE SCHEMA player`, causando conflito. Por isso o tracking usa `drizzle_player`. Ao adicionar novos serviços no mesmo Postgres, cada um precisa de seu próprio `migrationsSchema` distinto. · `player-store` `schema` `multi-tenant` `auth`
- **Todo atleta criado tem overall = 34 (pool fixo 56 + piso 20×4 = 136); diferem só no formato** — Calibração deliberada de justiça: `piso 20 · pool 56 · teto de criação 50` → soma dos 4 focos sempre 136 → overall = 34 = fundo da banda várzea (`WORLD.abilityByTier[3].min`, tier-4 = 34..66). `allocateAttributes` valida a soma fixa; qualquer mudança nos tunáveis (`PLAYER.creation`) deve recalibrar `CREATION_TOTAL = FOCI.length * floor + pool`. O campo `training_xp` (default 0) é o seam reservado para o card 13 (treino/evolução) incrementar sem tocar a criação. · `player` `attributes` `world-engine`
- **training-repo usa SELECT … FOR UPDATE para serializar read-modify-write do atleta** — loadActive (chamado por applyTraining e spendFreePoint) trava a linha do atleta com FOR UPDATE dentro da transação. Sem isso, dois escritores simultâneos sob READ COMMITTED perdem depósito/ponto (lost update). Mesmo padrão de integridade do publicador de rodada (SPEC-014/015). A matemática fica na lib pura (OP-17); o repo só lê/grava dentro da transação. · `player-store` `training` `concurrency`
- **joinTeamWithCode usa SELECT … FOR UPDATE; lockTeam não é transacional (gap conhecido)** — O join usa FOR UPDATE no team para serializar a corrida pela última vaga (até o 16º slot). lockTeam, porém, lê o captain e faz o UPDATE em dois statements separados — janela TOCTOU. O padrão do projeto para todo read-modify-write é transação (vide training-repo, joinTeamWithCode). Se lockTeam ganhar concorrência futura, envolver numa transação com FOR UPDATE. · `multi-tenant` `player-store` `team`
- **Posição de time = athlete.position; membros = atletas com team_id preenchido (sem tabela junction)** — Não existe tabela team_member. O atleta já é a linha: athlete.team_id FK→team (NULL = solo) e athlete.position = a vaga reivindicada no elenco. Consequência: converter um solo existente em membro de time não é suportado (bifurcação só no cadastro). A posição deve ser validada via isPosition() na borda antes de qualquer insert, pois a coluna não tem CHECK de enum no banco. · `player-store` `multi-tenant` `schema`
- **Penalidade de repetição só desacelera; rotação perfeita = pace da SPEC-017** — A penalidade por streak nunca acelera o XP acima do baseline plano (focusRepeatPct default 100). Rotação perfeita = ritmo idêntico à SPEC-017; martelar o mesmo foco só decai (piso garante progresso mínimo). As fronteiras de zona (104/204 pontos) e limiares (300/800/1500 XP) ficam intocados. · `training` `player` `banking`
- **seed + world_occupation = replayável; snapshot de atleta humano é cache derivada** — A fonte-da-verdade dos NPCs é o seed; a dos humanos é `world_occupation`. A linha `athlete` (`name`/`ability`/`is_human`) é cache derivada do overlay — nunca autoridade. Para reconstruir o mundo: re-semear NPCs e re-aplicar ocupações. `strength` nunca é persistida (continua derivada de `clubStrength` na leitura). · `world-store` `world-engine`
- **Regen só roda em gênese (sem rodada publicada) — reassignSlot também guarda** — O reassignSlot muta ability e age do atleta-mundo; se a temporada já publicou rodada, o snapshot seria corrompido (re-simulação reescreveria rounds publicados). Tanto occupyNpcSlot quanto reassignSlot aplicam assertGenesis. Regen mid-season é bloqueado por construção — candidato retenta na próxima gênese. · `regen` `world-store` `world_occupation`
- **Toda coluna nova em world_occupation deve ser adicionada manualmente em reapplyOccupations** — O rollover deleta e re-insere world_occupation com lista manual de colunas. Coluna ausente volta ao default a cada viragem. Este é o 2º caso aplicado (1º: regen_requested, SPEC-022; 2º: last_active_day + frozen_since_day, SPEC-023). Regra vale para qualquer coluna futura na tabela. · `world-occupation` `turnover` `world-store`
- **Economia: saldo só cresce via accrueRound — zero IAP/seam de moeda real** — O campo `athlete.balance` é incrementado EXCLUSIVAMENTE por `accrueRound` (salário `f(overall)` + prêmio de partida). Nenhuma função/rota pode adicionar saldo por dinheiro real. Invariante do charter (campo 05). Auditado em revisão; qualquer novo caminho que credite saldo deve ser rejeitado na revisão. · `economy` `player-store` `anti-dinheiro-real`
- **Nunca loja de stats: compras não escrevem em focos — trade-off é dado, não efeito** — `purchaseItem` e `accrueRound` não tocam os focos (fisico/tecnico/tatico/mental). O catálogo `PURCHASES` declara `tradeoff` como dado narrativo; a aplicação real é responsabilidade da 2.3/F2 via `aggregateTradeoffs`. Provado por teste dedicado em ambos os caminhos (compra e crédito). · `economy` `player-store` `nunca-loja-de-stats`
- **generateForDay usa lock advisory por (athleteId, day) para idempotência sem TOCTOU** — A geração do dia é envolta em `pg_advisory_xact_lock(hashtextextended('decision:{athleteId}:{day}', 0))`. Sem o lock, duas gerações concorrentes com contexto mudado no meio (ex.: compra sobe lifestyleTier) misturariam os conjuntos — o unique é por template, não por dia. A 1ª geração vence; a 2ª relê o existente. Nunca remova o lock sem cobrir esse TOCTOU. · `decisions` `player-store` `concurrency`
- **decision-repo NÃO lê injury-repo; injured chega como param extra (molde do age)** — O campo `injured?: boolean` entra em `DecisionContext` via `extra` de `generateForDay`, exatamente como `age`. O `decision-repo` nunca importa o `injury-repo`; quem wira é o scheduler/caller (lê `readInjuryState().available` e passa o flag). Esse padrão de seam-por-param deve ser mantido para qualquer estado externo futuro (moral, forma, etc.) — acoplar repos quebra a separação. · `decisions` `injury` `player-store`
- **trainFormaBump deve ser maior que decayStep para o treino render net-positivo** — `trainFormaBump=6 > decayStep=5`. Se o passe diário rodar no mesmo dia do treino, o net da forma é +1 (não zero). Se `trainFormaBump ≤ decayStep`, o treino não tem efeito visível no dia. Ao rebalancear os tunáveis em `MOOD` (`packages/player/src/mood.ts`), manter esta invariante. · `mood` `forma-moral` `player`
- **moodAbilityPct é simétrico: trunca a magnitude rumo a zero, não faz floor do valor com sinal** — Aplicar Math.floor diretamente sobre o quociente com sinal introduz viés pessimista (55/55 → +1 mas 45/45 → −2). A implementação correta: calcula a magnitude via Math.abs, aplica floor sobre ela, e depois restaura o sinal. Neutro (50/50) = 100 exato; 100/100 = 112; 0/0 = 88. · `forma-moral` `mood` `packages/player` [[SPEC-029]]
- **accrueRound só roda quando há rodada publicada (gate `paid`)** — O accrue (salário+prêmio) só é chamado quando `status ∈ {published, idempotent}`. Chamá-lo em dias de virada (`season_rolled`), deferred, locked ou before_season causa: (a) salário pago sem jogo, e (b) prêmio perdido no retry — o ledger reivindica o dia sem prêmio, e o retry acha o dia já reivindicado. A raiz é uma só: o gate `paid` em `runHumanPasses` (`services/scheduler/src/daily-tick.ts`). · `scheduler` `economy` `money-path` `player-store`
- **Colisão de nome: MatchResult no engine ≠ MatchResult no player-store** — `MatchResult` no `world-engine` é o registro da partida (`round, homeId, awayId, homeGoals, awayGoals, events?`). `MatchResult` no player-store é o outcome (`'win'|'draw'|'loss'`). No `daily-tick.ts` o tipo do engine é importado como `MatchRecord` para evitar colisão. Qualquer código que importe dos dois domínios deve aliasar um deles. · `world-engine` `scheduler` `daily-tick`
- **runRoundForDay re-lê mundo/âncora internamente a cada chamada (não recebe pré-computados)** — A função extraída é `runRoundForDay(db, seed, dayIndex, modulate?)` — relê `readWorld` + `readSeasonAnchor` internamente. Isso garante que após uma viragem o dia seguinte do loop já vê o mundo novo. A SPEC descrevia uma assinatura diferente (`publishRoundForDay` com world/results/startDayIndex externos) — o drift foi aceito porque a re-leitura interna é o comportamento correto para cross-season. Consequência: o catch-up re-simula `simulateWorldSeason` a cada dia do loop (não 1× por temporada como a SPEC pedia — drift aceito por simplicidade). · `scheduler` `world-store` `catch-up` `daily-round`
- **runAdmissionPass roda no FIM do processDay, depois de todos os passes** — A admissão da waiting-list deve ser o ÚLTIMO passo do processDay (após runVacancyPass, runRegenPass, runTransferPass). Se rodar antes: (a) as vagas revertidas no mesmo dia ainda não existem; (b) o admitido herdaria resultado e lesão da rodada já publicada do NPC que substituiu — uma partida que ele não jogou. No fim, ele entra e é processado apenas no próximo dia. · `scheduler` `waiting-list` `world-entry`
- **Regra de crescimento: alarga a base até 2× o andar acima, depois novo andar embaixo** — A entrada (tier de maior número) ganha +1 grupo por virada até `largura < branchingFactor(2) × largura(andar acima)`; quando satura, nasce um andar novo embaixo com largura 1 (nova entrada). Topo (tier 1) é sempre 1. Novos clubes usam a banda de várzea (última `abilityByTier`); clubes existentes não são re-ancorados. Índices globais de clube são CONTINUADOS da contagem atual (monotônico, sem colisão). Tunáveis: `WORLD.expansionThreshold=0.70`, `WORLD.branchingFactor=2`. · `world-engine` `expansion` `pyramid`
- **Todo lock no money path deve ser XACT-scoped (nunca de sessão) — invariante pooler-safe** — O endpoint pooled da Neon roda PgBouncer em transaction-mode: `pg_advisory_lock` de sessão, LISTEN/NOTIFY e SET SESSION quebram nesse modo. Todos os advisory locks do money path são `pg_advisory_xact_lock`/`pg_try_advisory_xact_lock`/`pg_advisory_xact_lock_shared` (liberados no commit). Introduzir um lock de sessão no futuro quebraria silenciosamente sob o pooler. Regra de revisão: nenhum `pg_advisory_lock` de sessão em `services/**`. · `neon` `world-store` `player-store` `money-path`
- **OP-09 pelo TIPO: JSON malformado no transporte nunca gera 400 antes da autenticação** — O transporte (`server.ts`) não valida conteúdo — JSON inválido vira `body: undefined` e a requisição segue para o roteador. Quem responde 400 é a validação do handler, após a sessão ser resolvida. Responder 400 no transporte preemptaria a autenticação (logout sem header + body quebrado deve ser 401, nunca 400). `requireSession` é `Handler → AuthedHandler` pelo tipo: rota protegida é inalcançável sem sessão viva, o handler não roda. · `auth` `api` `http`
- **semear mundo NUNCA sobrescreve — sem --force por design** — seedWorldOnce (harness/ops.ts) lança OpsError se a seed já existe, antes de qualquer escrita. Não existe flag --force. Razão: writeWorld sobre seed viva apagaria clubes, elencos, ocupações humanas e rodadas publicadas de todos os jogadores — a operação mais destrutiva do repo não pode caber num typo. Para recomeçar, apagar explicitamente no banco. · `harness` `world-seed` `ops`
- **Pools de camisa por posição (SHIRT.pools) são a única fonte de números válidos; colisão no elenco é aceita** — GK:[1,12] DEF:[2,3,4,5,6] MID:[8,10,14,16,18,20] FWD:[7,9,11,19,21] fallback:9. Todo número retornado deve estar no pool da posição. Unicidade entre jogadores do mesmo elenco é explicitamente dispensada — a faixa só mostra o SEU número; squad-uniqueness fica para um futuro roster view. · `shirt-number` `band` `athlete`

## 🎯 Decisões

- **Persistência in-memory com shim transacional; atomicidade de DB adiada para SPEC-002 (0.2)** — O spike prova o contrato do publicador (rollback total, nenhum leitor vê estado intermediário via begin/stage/commit/swap) mas NÃO prova atomicidade de banco. Concorrência real, durabilidade pós-crash e lock distribuído ficam explicitamente em aberto até 0.2. Registrado como claim honesto para não ser reaberto como dívida técnica surpresa. · `world-engine` `orchestration` `store`
- **Determinismo cross-plataforma: sem transcendentais, sem Intl/Date, golden commitado** — O motor usa PRNG uint32 puro (cyrb128+sfc32, saída x>>>0/2**32), CDF de Poisson pré-tabelada em inteiro (sem exp/log/pow) e âncora UTC-3 por aritmética de epoch (offset fixo -3h, sem Intl/ICU/Date). Motivo: float transcendental e ICU variam entre plataformas, corrompendo replay e auditoria. O golden (hash da temporada canônica + KAT do PRNG + vetor de âncora) é assertado no CI, provando determinismo cross-ambiente por construção. · `world-engine` `engine` `determinismo`
- **Animação WPF barata: usar TranslateTransform.X, não Canvas.Left — roda no render thread sem disparar layout** — Animar Canvas.Left dispara arrange na UI thread por frame. Animar TranslateTransform.X é processado no render/composition thread sem layout por frame. Resultado medido: CPU média 0,249% (~3% de 1 núcleo) com cena animada contínua em WPF, dentro do orçamento <1%. AllowsTransparency deve ficar desligado (composição por-pixel custa CPU). Footprint self-contained WPF: ~161 MB (trim não suportado no .NET 8) — eixo de decisão WPF vs Rust na feature #1. · `cliente` `faixa` `wpf`
- **Stack do cliente Windows ratificada: C#/WPF (.NET LTS)** — WPF é a stack F0 do cliente Windows. Único candidato medido; passou <1% CPU (0,249%, ~4× de folga) e <150 MB RAM (~87 MB, ~1,7× de folga) na SPEC-003 (Ryzen 5 5600X, 12 cores, Win11). Único con = footprint 161 MB self-contained (WPF não faz trim). Decisão reversível: cliente é thin renderer, zero regra de negócio (OP-17), re-port barato. Alternativas Rust/Tauri/WinUI3 avaliadas só por literatura — nenhuma foi medida. Raciocínio completo em docs/adr/ADR-001. · `cliente` `stack` `adr`
- **ToastNotificationManagerCompat (WCT 7.1.3) é a única opção viável para WPF unpackaged** — AppNotificationManager (WinAppSDK) arrasta o runtime WinAppSDK e falha em unpackaged self-contained (issue #6071). Windows.UI.Notifications cru exige escrever à mão AUMID + INotificationActivationCallback + registro COM — mais frágil sem upside. WCT auto-registra AUMID + COM activator em HKCU no primeiro Show(), NuGet puro, zero workload, build 0/0 com SDK 8.0.423. · `toast` `cliente-wpf` `cold-activation`
- **Render dentro da taskbar Win11 é no-go permanente; faixa ancorada é o caminho** — IDeskBand foi removido da taskbar XAML do Win11. Hacks restantes (reparent no HWND da taskbar, injeção em explorer.exe) quebram em updates e são flagados por AV — colide com 'zero anti-cheat' e <1% CPU. Widgets oficiais exigem MSIX e só aparecem no flyout Win+W. Decisão: implementar faixa compacta ancorada externamente (postura topmost ou AppBar), nunca renderizar dentro da shell. · `cliente` `taskbar` `windowing`
- **`WorldState` modela `tier → [leagues]` desde v1 para suportar grupos paralelos futuros** — Container é `Tier { tier, leagues: League[] }` + `League { leagueId, clubs }`. Em v1 há 1 league por tier; grupos paralelos (R13 elástico, disparado por ocupação humana) entram sem refatorar o tipo. Decidido para evitar quebra de API quando a pirâmide se ramificar. · `world-engine` `world-state` `multi-divisão`
- **Script de regen do golden mantido em harness/ (não deletado, não em packages/*/src)** — harness/regen-world-golden.ts reproduz exatamente o runChain() do world-turnover.test (seedWorld('decada') + 10 viragens coletando worldHash). Mantido e documentado — não throwaway — porque futuras mudanças intencionais de stream precisarão do mesmo fluxo. Vive fora de packages/*/src para não acionar o guardrail de determinismo (proíbe I/O/relógio nessa área). · `world-engine` `determinism` `harness`
- **world-store usa tsconfig.typecheck.json, não tsc -b reference** — Adicionar `./services/world-store` às references do `tsconfig.json` raiz falha com TS6310: projeto referenciado em `tsc -b --noEmit` não pode ter `noEmit:true`. Solução: world-store é incluído em `tsconfig.typecheck.json` (padrão do harness/), fora do grafo `tsc -b`. Quando um consumidor runtime surgir (Fatia 2+), converter para composite com alias próprio. · `world-store` `typecheck` `services`
- **Advisory lock xact-scoped (pg_try_advisory_xact_lock) em vez de lock in-process** — publishRound usa pg_try_advisory_xact_lock(hashtextextended(key, 0)) dentro da transação Drizzle. Lock é xact-scoped: auto-liberado no COMMIT/ROLLBACK pelo Postgres, sem finally manual, sem leak. Mapeia diretamente a semântica do Set in-process da SPEC-002. Localizado em services/world-store/src/store/round-repo.ts. · `world-store` `published_round` `round-repo`
- **Grão de atomicidade da rodada diária = MUNDO INTEIRO (publishWorldRound)** — Todas as ligas publicam numa única transação (advisory lock world-day 'world:{seasonId}:{round}', INSERT multi-linha, seam onBeforeCommit). Falha → ROLLBACK total, nenhuma liga publicada. publishRound por-liga (Fatia 2) permanece como primitivo para Copa/ops manual. Decisão do founder: charter exige 'a linha do tempo do mundo é all-or-nothing'. · `round-repo` `publishWorldRound` `world-store`
- **argon2id e hashing vivem exclusivamente em `services/*`; a política de senha (tamanho) fica em `packages/player`** — O guardrail de determinismo (ESLint) proíbe `Math.random` e chamadas impuras em `packages/*/src`. Salt aleatório do argon2 é impuro → `hashPassword`/`verifyPassword` só existem em `services/player-store/src/store/auth.ts`. A validação de tamanho mínimo (≥10) é pura e fica em `packages/player/src/password-policy.ts`. Nunca mover hashing para a lib pura. Lib usada: `@node-rs/argon2` (napi, binários prebuilt — sem node-gyp). · `auth` `player` `player-store` `determinism`
- **Progressão do atleta: Model A — barra única + ponto flutuante, curva piecewise de 3 zonas** — O limiar de XP para o próximo ponto livre cresce em 3 zonas (piecewise inteira, sem transcendentais — guardrail proíbe exp/pow em packages/*/src): ~300 XP/ponto na várzea (p<104), ~800 no meio (104≤p<204), 1500+ramp na elite. `pointsEarnedTotal = soma_focos − 136 + freePoints` inclui pontos não gastos (anti-hoarding: acumular pontos não barateia o próximo). A primitiva de gasto é `applyPoint` (+1, teto 99) — distinta de `allocateAttributes` (trava de criação, exige soma===136); o treino cresce a soma além de 136. · `player` `progression` `training`
- **Geração de código aleatório vive exclusivamente no serviço, nunca na lib pura** — O guardrail proíbe Math.random/Date/Intl em packages/*. generateCode usa node:crypto.randomInt e fica em services/player-store/team-repo.ts. A lib só valida a forma (validateCodeFormat). Unicidade garantida por pré-checagem + retry (até 8x) + UNIQUE constraint como rede final. Alfabeto sem ambíguos: sem O/0/I/1/L (TEAM.code.alphabet). · `player` `player-store` `guardrail`
- **Penalidade entra via TrainOpts.focusRepeatPct (seam neutro) para preservar os 168 testes da SPEC-017** — TrainState e TrainResult foram declarados imutáveis (testes usam toEqual exato). A penalidade de streak é injetada como terceiro multiplicador percentual inteiro em sessionDeposit via TrainOpts.focusRepeatPct (default 100). Chamadas sem esse campo continuam produzindo resultado idêntico à SPEC-017. O streak em si vive no player-store (last_focus + focus_streak), não na lib pura. · `training` `player` `player-store`
- **world_occupation carrega ability e human_name congelados — replay honesto** — O `ability` do humano no mundo é uma foto do momento da ocupação (derivada de focos mutáveis do treino). Se o overlay não armazenasse os valores congelados, re-aplicar ocupações sobre um seed recriado produziria dados diferentes dos originais. Por isso `world_occupation` persiste `human_name` + `ability` (e não os recupera do player-store no replay). · `world-store` `multi-tenant` `schema`
- **Imunidade do humano no engine via `immuneIds` (ids, não flag no Athlete)** — A imunidade à aposentadoria/transferência na viragem entra como 4º arg opcional `immuneIds: ReadonlySet<string>` em `advanceWorld`. O engine fala de ids, não de 'humano' — mantém o domínio puro desacoplado. Alternativas descartadas: campo `Athlete.isHuman` (regeneraria o golden por serialização mesmo com zero humanos) e re-apply na camada de dados (lossy). Set vazio = stream e JSON byte-idênticos ao estado anterior. · `world-engine` `world-turnover` `rollover`
- **reassignSlot atômico em vez de vacate→reoccupy no Regen** — A ordem original (deletar world_occupation e recriar) abria janela órfã irrecuperável: um crash entre os dois passos deixava o humano fora do mundo permanentemente, pois readRegenEligible deriva de world_occupation. Fix definitivo: reassignSlot reaponta a MESMA linha ao renascido numa única transação — a ocupação nunca é deletada. Qualquer falha antes do reassign deixa o candidato elegível (idade não resetada) e o próximo passe o reencontra. · `regen` `world-store` `world_occupation`
- **Outcomes de decisões são seam declarado — nenhum atributo/saldo é alterado pelo motor** — O motor (SPEC-025) grava o `outcome` como dado jsonb no log; a aplicação real é de outro sistema: moral/forma → 2.3 (Forma/Moral), transferência → card 1.4 (Transferências). Decisão explícita do founder para manter a fatia só-player-store e não duplicar o roadmap. O `outcome.transfer` registra o clube-alvo; nada move no world-store. · `decisions` `player-store` `multi-tenant`
- **Modelo de barra = baseline + offset permanente + evento que decai** — Forma/Moral são [0,100], baseline 50. Compras possuídas geram um offset permanente na Moral (clampeado ±30 por `lifestyleClamp` — comprar tudo não estoura). Eventos (decisão, comeback, treino) entram como bumps na fonte, dentro da transação do repo que gerou o evento. O passe diário (`applyDailyMood`) só faz o decay rumo ao alvo via `stepToward` (monotônico, sem overshoot). Isso evita a barra grudar em extremos e modela 'posse contínua vs. evento pontual' sem event bus. · `mood` `player-store` `forma-moral`
- **Forma/Moral modulam a ability do humano na partida via seam injetado (peso 60/40, ±12%)** — runDailyRound ganhou 4º param opcional `modulate?: WorldModulator` (default identidade), aplicado entre readWorld e simulateWorldSeason. A costura cross-schema (world-entry/mood-modulator.ts) é injetada pelo scheduler — o world-store não importa o player-store. Peso formaWeight=6/moralWeight=4, matchSwingPct=12 (todos tunáveis em MOOD). Sem modulate: comportamento idêntico, testes NPC intocados, 4 goldens byte-idênticos. · `forma-moral` `world-store` `daily-round` `world-entry` [[SPEC-029]] [[SPEC-027]] [[SPEC-020]]
- **Ledger de idempotência: INSERT-onConflict atômico dentro da tx do repo** — O padrão escolhido para tornar `accrueRound` e `applyDailyMood` idempotentes é um INSERT em `daily_ledger (athlete_id, day, scope)` com `onConflictDoNothing().returning()` dentro da mesma transação do efeito. 0 linhas retornadas = já executou hoje → skip. Crash antes do commit des-reivindica (rollback). Escopo `'accrue'` e `'mood'` são os únicos — os demais passes (decisão/recuperação) já eram retry-safe e não usam o ledger. Molde do `decision_one_per_day`. · `player-store` `daily-ledger` `idempotency` `scheduler`
- **Eventos de partida enriquecem no world-season com RNG separado — resolveMatch/simulateSeason e os 4 goldens intocados** — O enriquecimento de eventos (lesões, futuros gols/escolhas) vive em `world-season.ts` — única camada que tem `WorldClub.roster`. O RNG de eventos é derivado com discriminador `'events'` (`deriveSeed(seed, leagueId, seasonId, round, homeId, awayId, 'events')`), stream totalmente separado do placar. Consequência: `resolveMatch`/`simulateSeason` não são tocados, `season.golden.json` e `world.golden.json` ficam byte-idênticos. Qualquer evento futuro (`goal`, `choice`) deve seguir o mesmo padrão: enriquecer pós-placar no world-season com sub-seed próprio. · `world-engine` `match-events` `world-season` `golden`
- **transferOccupation usa clear-first (AT-MOST-ONCE) para evitar move em dobro** — A flag `transfer_requested` é limpa ANTES de `transferOccupation`, não depois. O move não é idempotente (re-escolhe destino excluindo o clube atual), então um crash entre move e limpeza moveria o humano uma segunda vez. Clear-first troca isso por 'perde a transferência num crash raro' (re-ofertada na próxima viragem), nunca corrompe o snapshot. A SPEC descrevia a ordem inversa mas foi corrigida na revisão adversarial. · `transfer` `world-store` `occupation`
- **Transferência usa a força VIVA (abilityFromFocos) — não a ability congelada da ocupação** — A proposta gatilha no overall vivo (focos atuais). O destino e a nova vaga devem usar a mesma base, senão um humano que cresceu no treino teria a transferência evaporada (congelada 34 não acharia destino). A costura lê `abilityFromFocos` dos atributos atuais e a usa no pick e no `transferOccupation.ability`. A ability congelada no `world_occupation` original é ignorada para este fim. · `transfer` `occupation` `player`
- **allowMidSeason na entrada solo: a guarda de gênese era conservadora demais** — occupyNpcSlot tinha assertGenesis que barrava entrada mid-season. A guarda foi provada desnecessária: publishWorldRound é idempotente por (season_id, round) e nunca reescreve rodadas passadas; occupyNpcSlot só muta o input futuro. O único trade-off real é a tabela da viragem re-simular com o entrante (diverge das rodadas publicadas do clube) — money-path-neutro, mesma classe do moodModulator (SPEC-029). Fix: OccupyInput.allowMidSeason? em occupation-repo.ts; regen/transfer ficam gênese-gated. · `world-store` `occupation-repo` `world-entry`
- **Pirâmide elástica: topologia cresce por dispatch + gating, nunca por migration** — A expansão da pirâmide (R13) é gated no 5º param `expand?` de `advanceWorld` (default `false`, zero PRNG consumido → goldens byte-idênticos). A promoção usa dispatch: todos os tiers com 1 grupo → `applyPromotionRelegation` v1 intocado; qualquer tier com grupos paralelos → `promoteRelegateMulti`. Sem migration porque `overwriteSnapshot` já persiste topologia variável (SPEC-009/013/021). O mesmo padrão de injeção positional do `immuneIds` (SPEC-021). · `world-engine` `world-store` `pyramid` `promotion`
- **Migrations no endpoint direct (DATABASE_URL_UNPOOLED); runtime no pooled (DATABASE_URL)** — O migrator do drizzle e DDL não devem depender do PgBouncer (transaction-pooling pode interferir). `pickMigrationUrl(env)` usa `||` (não `??`): um `DATABASE_URL_UNPOOLED` vazio cai na `DATABASE_URL` — strings vazias não são endpoints válidos. CI escreve ambas via `$GITHUB_ENV` (não no env do job) para evitar o gotcha de precedência job-env × `$GITHUB_ENV`. Ver `migrate.ts` (world-store e player-store) e `docs/adr/ADR-002`. · `neon` `world-store` `player-store` `ci`
- **Sessão opaca server-side sem par access/refresh (ADR-003)** — Token opaco 256 bits (CSPRNG base64url), apenas sha256hex persistido em `player.session`. Sem JWT/refresh porque: (a) revogação é requisito funcional — SPEC-022/023 encerram carreira/vaga e precisam invalidar sessões imediatamente; (b) zero dep e zero segredo novo; (c) o par access/refresh só existe para compensar irrevogabilidade de JWT stateless — com sessão em tabela a linha É o mecanismo. TTL idle 7d + absoluto 30d + logout deleta linha + cap 10 por conta. Contradiz âncora ratificada anterior em `sdd.md:80` — atualizada explicitamente (P5b). Ver ADR-003. · `auth` `session` `player-store` [[ADR-003]]
- **data→dayIndex via resolveSlot do engine; seasonId derivado do mundo** — set-anchor recebe uma data YYYY-MM-DD (não dayIndex) e converte montando 15h BRT (= 18:00 UTC, offset fixo -3) e passando por resolveSlot (anchor.ts:22). Nunca reimplementar a aritmética de fuso: divergência faria o mundo jogar num dia e o tick esperar outro. O seasonId é lido de readWorld — operador nunca o informa, impossível ancorar temporada errada. · `harness` `season-anchor` `world-engine` `ops-date`
- **Número da camisa é DERIVADO da posição — sem escolha do jogador, sem coluna, sem migration** — Decisão do founder (SPEC-040): `shirtNumber(position, athleteId)` é fn pura em `packages/player/src/shirt-number.ts`. Reverte (a) o seletor 1–99 do design handoff v3 e (b) a decisão da SPEC-038 ('o jogador escolhe'). O número é estável porque posição é fixa na criação (SPEC-016) e athleteId é imutável — recomputável, nunca persistido. · `athlete` `band` `shirt-number` `packages/player` [[SPEC-038]] [[SPEC-016]]

## ⚠️ Gotchas (comportamento inesperado a não reaprender)

- **Win+D (mostrar desktop) no Win11 usa DWM cloaking — não interceptável por mensagem de janela** — No Windows 11, Win+D esconde janelas via DWM cloaking sem disparar WM_SHOWWINDOW, WM_SIZE ou SWP_HIDEWINDOW. Barrar SWP_HIDEWINDOW ou engolir SC_MINIMIZE não resolve. A solução é parentar a faixa à camada WorkerW do desktop (técnica Wallpaper Engine/Lively), mas no Win11 o SendMessage 0x052C não spawna a WorkerW separada como no Win10 e o SetParent no Progman não persiste — trabalho não-trivial, deferido ao cliente real. · `cliente` `faixa` `win32`
- **ClipToBounds no elemento <Window> do WPF é propriedade proibida — causa crash de startup** — WPF lança InvalidOperationException ao parsear XAML se ClipToBounds='True' estiver no elemento Window (proibido neste nível). O processo crashava com exit 0xE0434352 em 100% das execuções; o WER segurava o processo ~30–60 s mascarando o crash como 'processo vivo'. Fix: remover do Window — ClipToBounds pertence ao Canvas/painel interno. · `cliente` `faixa` `wpf`
- **Gate items de WPF NÃO foram executados antes da ratificação** — Três gates do cliente real foram explicitamente deferidos e não bloqueiam a ratificação, mas são obrigatórios antes do GO definitivo: (1) soak de 8 h (`measure-usage.ps1 -Seconds 28800`) para provar ausência de leak de GC; (2) validação de CPU em hardware fraco (4–8 cores — o 0,249% é de máquina num box de 12 núcleos; custo por-core é 2–4× maior com menos núcleos); (3) WorkerW/Win+D — no Win11, 'mostrar desktop' usa DWM cloaking e esconde a faixa; solução é parentar à WorkerW. Também: code-signing para founder BR tem gap (Azure Trusted Signing individual é US/CA apenas — resolver via org/PJ ou cert OV na SPEC de distribuição). · `cliente` `stack` `distribuicao`
- **SHQueryUserNotificationState não cobre borderless-fullscreen nem DND manual no Win11** — Browser-fullscreen (DWM/borderless) não vira o QUNS; DND manual do Win11 é tratado pela plataforma de notificação (o Windows suprime o banner sozinho, não expõe via QUNS). Windows.UI.Notifications.NotificationMode não existe nessa projeção WinRT e não compila. Detecção fina de borderless-fullscreen exige heurística suplementar (foreground cobre monitor + topmost). Fail-open é a política correta: perder o ritual é pior que um toast ocasional. · `toast` `notification-gate` `silencio`
- **ToastScenario.Reminder obrigatório para cold-click em botão funcionar** — Sem ToastScenario.Reminder, o Win11 recolhe o toast na Central de Ações e esconde os botões; o body-tap ativa com argument vazio (decisão perdida). Com Reminder o banner fica fixo com botões visíveis até o usuário decidir — é o único cenário que garante que cold-click num botão carregue a decisão correta. · `toast` `cold-activation`
- **AppBar (SHAppBarMessage) reserva e libera a work-area com latência de ~15-30s no Win11 26200** — Após ABM_SETPOS a banda posiciona instantaneamente, mas o encolhimento da work-area pelo shell assenta ~15–30s depois. ABM_REMOVE idem. Sem leak na saída graciosa — é lentidão do shell, não do código. Consequência: medir a work-area cedo demais dá falso negativo. Enfraquece a vantagem única da postura B (reserva 'instantânea') em favor da postura A (topmost). · `cliente` `taskbar` `windowing`
- **CloseMainWindow() é no-op em janela WS_EX_TOOLWINDOW + ShowInTaskbar=false; Stop-Process -Force vaza a reserva AppBar** — Janelas com WS_EX_TOOLWINDOW e ShowInTaskbar=false não têm MainWindowHandle no WPF — CloseMainWindow() não faz nada. Para fechar graciosamente (disparando Closing → ABM_REMOVE) é preciso enviar WM_CLOSE direto ao HWND via EnumWindows por PID. Stop-Process -Force (TerminateProcess) é ininterceptável: pula o ABM_REMOVE e a reserva de borda vaza até restart do explorer. · `cliente` `taskbar` `windowing`
- **`archetype` e `weights` do clube devem ser sorteados ANTES do elenco na geração** — Em `seedWorld`, o PRNG precisa sortear `archetype`/`weights` do clube antes de gerar os atletas do elenco. Inverter a ordem desloca o stream e quebra golden/replay — mesmo que archetype não seja usado na v1. Não é opcional. · `world-engine` `world-seed` `prng`
- **Mudar tunável de elenco/transferências quebra world.golden.json — regen obrigatória e isolada** — Reduzir rosterSize ou transfersPerLeague muda os saques do PRNG dentro de cada clube e na viragem, alterando todos os 11 hashes do world.golden.json. Os outros três goldens (season/prng/anchor) são independentes de roster e devem permanecer bit-idênticos (git diff=0) — diferença nesses é sinal de vazamento para o money-path escalar. Script canônico de regen: harness/regen-world-golden.ts (fora de packages/*/src para não violar o guardrail de determinismo do lint). · `world-engine` `determinism` `golden-fixtures`
- **Driver Postgres deve ser pooled/TCP — nunca HTTP one-shot** — O driver HTTP one-shot da Neon não suporta transação interativa multi-statement. A Fatia 1 já usa `pg.Pool` (pooled/TCP) para garantir que a Fatia 2 (publicador all-or-nothing com BEGIN/COMMIT) não exija re-tooling de driver. · `world-store` `database` `services`
- **Chave de rodada sem world_seed — unicidade cross-mundo é evolução futura** — PK composta de published_round é (league_id, season_id, round), sem world_seed. Fiel à SPEC-002/card. Quando múltiplos mundos coexistirem a mesma (league_id, season_id, round) pode colidir entre mundos distintos — a chave precisará ser expandida. Não fazer isso nesta fatia foi decisão explícita. · `world-store` `published_round` `schema`
- **start_day_index é input de ops obrigatório; sem ele o tick falha com sem_ancora** — O snapshot (readWorld) expõe season_id mas NÃO o dayIndex do round 1. A tabela season(world_seed, season_id, start_day_index) guarda esse dado via setSeasonAnchor — pré-condição do tick. Re-ancorar após dia adiado é upsert deliberado (nunca automático). vitest.config.ts requer fileParallelism:false porque as suítes de integração compartilham um Postgres e truncam tabelas comuns. · `season-repo` `daily-round` `world-store` `schema`
- **Calibração de curva: ritmos 3/8/15 e 'carreira → ~85 overall' são mutuamente incompatíveis com os tunáveis atuais** — Com sessionXp=100 e limiares 300/800/1500, chegar a overall 85 custa ~1112 treinos (~1,5 carreiras), não 440. Uma carreira de pico (~440 treinos) chega a ~overall 64. Decisão do founder estava pendente no merge. Toda a calibração vive em TRAINING (constants.ts) — reequilibrar é só mudar tunáveis + ajustar valores esperados nos testes de ritmo, sem tocar lógica. · `player` `training` `calibration`
- **repeats ≠ focus_streak persistido: repeats é o histórico usado na penalidade, focus_streak é o próximo estado** — resolveFocusStreak devolve repeats (sessões consecutivas JÁ feitas, usado para calcular a penalidade desta sessão) e o próximo focus_streak a persistir (+1 se repetiu, 1 se trocou). Confundir os dois faz a penalidade ser aplicada com um ciclo de atraso ou adiantamento. · `training` `player-store`
- **occupyNpcSlot toma advisory lock COMPARTILHADO para serializar contra publishWorldRound** — A guarda da gênese tem TOCTOU: a temporada pode passar de gênese para em-andamento entre o `assertGenesis` e o commit. A ocupação toma `pg_advisory_xact_lock_shared(hashtextextended('world:{seasonId}:1', 0))`; `publishWorldRound` toma o exclusivo na mesma chave. Shared×shared não bloqueia (ocupações concorrentes seguem); shared×exclusive serializa contra a publicação da rodada 1. Sem esse lock a guarda é falha. · `world-store` `occupation-repo`
- **`runTransfers` suprime o swap mas NÃO pula os saques do PRNG (stream-preserving)** — Quando um dos dois atletas sorteados é imune, o swap é anulado mas `pickTwoDistinct`/`pick(POSITIONS)`/`pickPositionIndex` já rodaram — o stream do PRNG avança igualmente. Com set vazio nunca suprime → byte-idêntico ao golden. Qualquer refactor que mova a guarda para antes dos saques QUEBRA o determinismo mesmo sem humanos no cenário. · `world-engine` `transfers` `prng`
- **Âncora nova da temporada = `dayIndex + 1` (dia REAL da virada), não calendário ideal** — Se a virada for deferida (falha transitória → retry N dias depois) e a âncora nova usar `oldStart + roundsLength + 1`, ela aponta para um dia já passado e as rodadas iniciais da nova temporada nunca são publicadas. A correção: `newStart = dayIndex + 1`, onde `dayIndex` é o dia REAL em que `persistWorldTurnover` executa. `persistWorldTurnover` recebe `dayIndex` explicitamente por isso. · `rollover` `world-store` `season` `daily-round`
- **Flag regen_requested deve ser preservada na virada (reapplyOccupations)** — O passe de Regen roda pós-virada, mas reapplyOccupations (SPEC-021) re-inseria world_occupation sem regen_requested, zerando a flag a cada virada. Resultado: gatilho voluntário (25–41) nunca dispararia. Fix: OccupationView e reapplyOccupations passam a carregar e re-inserir regenRequested. · `regen` `world-store` `turnover`
- **runVacancyPass decide no snapshot mas muta com re-check condicional atômico (anti-TOCTOU)** — O passe lê ocupações sem lock (snapshot), mas freezeOne e revertIfStale re-checam last_active_day ao vivo no próprio WHERE da mutação. Sem isso, um markActive concorrente (humano voltando no dia 30) era atropelado e o humano era expulso irrecuperavelmente. Como last_active_day só cresce, uma decisão obsoleta vira no-op seguro. · `vacancy` `world-occupation` `world-store`
- **onFreeze/onThaw disparam DENTRO da transação — lançar faz rollback e o próximo passe retenta** — O hook de notificação fica dentro da tx de freeze/thaw. Se lançar, o frozen_since_day não persiste e o próximo passe reencontra a vaga como não-congelada e retenta (at-least-once). Um sender real deve usar outbox (grava na tx, envia async) — nunca bloquear a tx com SMTP síncrono. · `vacancy` `world-store` `notification`
- **accrueRound sem chave de idempotência: retry = pagamento em dobro — obrigatório resolver no scheduler** — `accrueRound(athleteId, result?)` não carrega identificador de rodada (`season_id`/`round`), logo um retry do scheduler re-credita salário+prêmio. A idempotência é OBRIGATÓRIA no card do scheduler (ledger `(athlete_id, season_id, round)`, molde do `publishWorldRound`). Não costurar `accrueRound` ao tick antes de adicionar essa chave. · `economy` `scheduler` `player-store`
- **Nova tabela-filha no player-store exige atualizar delete-order em TODA suíte que apaga o pai** — A tabela `purchase` (FK→`athlete`) fez testes de player-repo, training-repo, team-repo, regen e world-entry violarem FK ao apagar `athlete` sem apagar `purchase` antes. Regra: ao criar tabela-filha, adicionar `delete(filha)` antes de `delete(pai)` em TODOS os `beforeEach`/`wipeAll` do projeto, não só no novo teste. · `player-store` `schema` `test`
- **Coluna `ord` é obrigatória para reproduzir a ordem do dia; wipeAll deve deletar `decision` antes de `athlete`** — O multi-row INSERT compartilha `created_at`, tornando a ordem não-determinística sem `ord` (rank do hash FNV persistido). `readDecisionLog` e o read-back ordenam por `(day DESC, ord)`. Além disso, `decision` tem FK para `athlete` — qualquer `wipeAll`/`beforeEach` deve deletar `decision` antes de `athlete` (e `purchase`), ou viola a constraint. Padrão aplicado nas 6 suítes irmãs (player-repo, team-repo, training-repo, economy-repo, enter-world, regen). · `decisions` `player-store` `schema`
- **injureFromMatch usa lock advisory + fecha-lazily lesão vencida (status≠dia)** — Havia inconsistência entre dois seams: `readInjuryState.available` é derivado pelo DIA (arco puro), mas o guard '1 ativa/atleta' de `injureFromMatch` era por STATUS (índice parcial). Uma lesão com prazo vencido mas `status=active` (scheduler não rodou `advanceRecovery`) bloqueava a nova em silêncio. Fix: dentro da tx, `injureFromMatch` adquire `pg_advisory_xact_lock` no atleta e faz UPDATE lazy fechando qualquer lesão onde `started_day + recovery_days <= day` antes de checar/inserir. Sempre que mexer em injury-repo, lembrar que status e fase-por-dia são dois relógios que precisam ser reconciliados. · `injury` `player-store` `multi-tenant`
- **Transição active→recovered tem dois produtores: advanceRecovery E o lazy-close de injureFromMatch** — Quando uma nova lesão chega antes do passe diário, `injureFromMatch` fecha-lazily a lesão vencida (UPDATE WHERE status=active AND vencida). Se só `advanceRecovery` aplicava o comeback de moral, a re-lesão engolia o bônus. Fix: ambos os produtores da transição aplicam `bumpMoral(comebackOutcome().moral)` na mesma tx — o segundo produtor casa 0 linhas e é no-op. Ao adicionar qualquer novo produtor da transição `active→recovered`, aplicar o comeback na mesma tx. · `mood` `injury-repo` `player-store`
- **Replay-dos-inputs quebrado após SPEC-029: forma/moral não são snapshotados por rodada** — A rodada publicada é durável (published_round), mas não é mais recomputável a partir de (seed + base congelada) — depende de forma/moral do momento, que o passe diário sobrescreve logo depois. Se o replay-dos-inputs for exigido, um snapshot de forma/moral por rodada é necessário. Débito explícito sinalizado; ainda não corrigido. · `forma-moral` `daily-round` `replay` `audit` [[SPEC-029]] [[SPEC-020]]
- **daily_ledger deve ser deletada antes de athlete em todo wipeAll de teste** — `daily_ledger` tem FK para `athlete` (ON DELETE no action). Qualquer suíte que limpa o player-store deve fazer `delete(daily_ledger)` antes de `delete(athlete)`, caso contrário o delete do athlete viola a FK e quebra o setup. Afeta todas as suítes irmãs que tocam player-store. · `player-store` `daily-ledger` `test`
- **Injeção de lesão no tick deve ser isolada (tryInjure) — uma falha não pode starvar os demais passes do humano** — Chamar `injureFromMatch` diretamente dentro de `runHumanPasses` sem try/catch faz uma gravidade corrompida no jsonb (ou falha transitória) abortar mood/decisões/recuperação do humano no dia. A solução (`tryInjure`) envolve a chamada em try/catch com log OP-11 e retorna `false` em caso de erro — a lesão é best-effort, os demais passes seguem. Padrão a replicar para eventos futuros injetados pelo tick. · `scheduler` `daily-tick` `injury`
- **.dockerignore deve ficar na RAIZ do repo quando o contexto de build é a raiz** — Um `.dockerignore` em `services/scheduler/` é INERTE quando o build é `docker build -f services/scheduler/Dockerfile .` (contexto = raiz). O Docker lê o `.dockerignore` da raiz do contexto. O padrão `.env` sem `*` também não cobre `.env.production`/`.env.local`. Fix: `.dockerignore` na raiz com `**/.env` e `**/.env.*` (com `!.env.example`). O Dockerfile é single-stage (não multi-stage como a SPEC pedia) e roda via `tsx` — otimização para imagem mínima é card futuro. · `scheduler` `docker` `deploy`
- **pickTransferDestination deve excluir slots humanos ao buscar a vaga mais fraca** — O `WorldState` não tem campo `isHuman` nos atletas do roster. Se `weakestNpcAbilityAt` não receber o conjunto de `humanSlotIds` e excluí-los, o pick pode contar um humano fraco como 'vaga NPC mais fraca', enfraquecendo o destino (trocaria um NPC forte) ou lançando erro (sem vaga NPC disponível) — deixando a proposta presa. A correção: a costura lê ocupações frescas por-candidato e passa o set de slots humanos ao pick. · `transfer` `world-store` `occupation`
- **admitOrEnqueue não fura a fila: se há contendor da mesma posição esperando, o novo entra atrás** — Mesmo que countEntryHumans < cap, se já existe alguém na waiting_list com a mesma posição o novo candidato vai para a fila (não entra imediato). Sem essa regra o imediato fura a FIFO de quem já esperava a mesma vaga. Implementado em admission.ts: readQueue → contested = queue.some(q => q.position === identity.position). · `waiting-list` `world-entry` `admission`
- **entryOccupancyRate: numerador = humanos (não clubes distintos), denominador = slots totais da entrada** — `entryOccupancyRate(world, occupations)` conta CADA ocupação individualmente (múltiplos humanos no mesmo clube somam) dividido por `nº de clubes da entrada` (não por vagas de posição). A função é pura (recebe snapshot + lista de ocupações, sem query). O gatilho `>= 0.70` dispara na `persistWorldTurnover`; sem humanos (beta cedo) o mundo permanece linear — comportamento correto. · `world-store` `turnover-repo` `expansion`
- **pg deriva SSL da connection string — o objeto `ssl` explícito é sobrescrito quando a URL tem `sslmode`** — Em `node-postgres`, o parse da connection string faz `Object.assign({}, config, parse(url))`: o campo `ssl` da URL vence o objeto explícito passado ao Pool. Por isso as URLs Neon devem usar `sslmode=verify-full` (não apenas `require`) — um bump futuro do pg para v9 degradaria `require` a no-verify. O objeto `ssl:{rejectUnauthorized:true}` de `buildPoolConfig` só é honrado quando a URL não tem `sslmode` (ex.: host `.neon.tech` sem query string). Ver ADR-002 e `buildPoolConfig` em `client.ts`. · `neon` `world-store` `player-store`
- **Dois relógios na mesma entidade de sessão causam TTL divergente** — Em `createSession`, `created_at`/`last_seen_at`/`expires_at` devem ser todos gravados a partir do `nowMs` injetado, nunca misturando `defaultNow()` do Postgres com o relógio da aplicação. O bug real: `expires_at` vinha do `nowMs` injetado, mas `created_at`/`last_seen_at` usavam `defaultNow()` do banco — sob clock skew ou testes determinísticos os TTLs relativos entre si divergiam silenciosamente. Regra: numa borda com tempo injetado, o default do banco é segunda fonte de verdade. · `session` `player-store`
- **harness/ não estava no include do vitest — adicionado na SPEC-039** — O vitest.config.ts cobria só packages/*/src e services/*/test. Testes em harness/ existiriam e nunca rodariam. Resolvido adicionando 'harness/**/*.test.ts' ao include. Se criar novos testes fora desses três globs, verificar o include antes. · `harness` `vitest` `test-infra`
- **FNV-1a em shirt-number.ts é replicado (não importado) — hashes de kit.ts/decisions.ts são privados** — O guardrail de `packages/*/src` proíbe random/relógio; o FNV-1a de `decisions.ts` e `kit.ts` é privado (não exportado). Por isso `shirt-number.ts` replica o FNV-1a + avalanche xorshift localmente. Se alguém quiser DRY no futuro, precisará extrair um `hash.ts` compartilhado — mas até lá a replicação é intencional. · `shirt-number` `packages/player` `guardrail`

## 📎 Referências

- **Tabela injury: índice único parcial (athlete_id) WHERE status='active' — migration 0006** — Schema `player`, arquivo `services/player-store/src/migrations/0006_injuries.sql`. Garante 1 lesão ativa/atleta no banco. `severity` é `text` sem CHECK de enum — validação fica na borda via `isSeverity()` (padrão do projeto, igual a `position`). FK para `athlete.id` sem ON DELETE CASCADE: ao deletar atleta em testes, deletar `injury` ANTES (ordem FK: injury → decision → purchase → athlete). · `injury` `schema` `player-store`
- **tick_progress: cursor resumível do catch-up (world-store migration 0007)** — Tabela `tick_progress(world_seed PK, last_day_index)` guarda o último dayIndex cuja RODADA DO MUNDO liquidou. O cursor rastreia a rodada do mundo, não o humano — um humano quebrado não trava o batimento. `advanceTickCursor` usa upsert com `greatest` (monotônico, nunca retrocede). Arquivos: `services/world-store/src/schema/tick-progress.ts`, `tick-progress-repo.ts`, migration `0007_tick_progress.sql`. · `scheduler` `world-store` `cron` `catch-up`
