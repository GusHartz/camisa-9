# CLAUDE.md — camisa-9

## O que é este projeto

**camisa-9** (nome de fundação: Nexus Flow / H1VE) é um jogo de carreira de futebol de **baixa atenção** que roda numa faixa discreta acima da taskbar, enquanto o usuário trabalha no desktop.

**A visão em uma frase:** você vive a carreira de UM jogador de futebol — da várzea às lendas — numa faixa acima da taskbar, no mesmo time dos seus amigos, num mundo persistente que joga **TODO DIA às 15h (Brasília)** com ou sem você.

**O problema.** Quem passa 6–10h/dia no computador e ama futebol só consome o esporte de forma passiva no expediente (placar, zoeira no grupo, portal). Jogos de futebol exigem atenção integral (Football Manager, tela cheia) ou vivem no celular com monetização odiada (ads). Existe uma lacuna verificada: o gênero de *desktop ambiente* (Rusty's Retirement, TBH) é demanda massiva, mas 100% solitário — sem servidor, sem eventos, sem esporte. A interseção **ambiente + futebol + social síncrono está vaga**.

**Quatro pilares da tese:**
- **Presença ambiente** — o jogo trabalha *junto* com o expediente, nunca contra. Promessa pública: **<1% CPU, zero anti-cheat no cliente**.
- **Cooperação, não gestão** — você **é** o atleta; seus amigos estão no **mesmo** time. O clube é palco (NPC), não propriedade.
- **Ritual coletivo sincronizado** — jogo diário (7/7) às 15h Brasília (liga de 20, 38 rodadas ≈ 6 semanas), redirecionando o pull de "conferir o placar".
- **Mundo vivo com história permanente** — nasce 100% NPC; cada humano substitui um NPC (escassez via waiting list); carreiras terminam e viram lendas permanentes.

**North star:** times com **≥3 humanos presentes** no jogo das 15h.
**Guardrails:** D30 ≥30% · presença ao vivo ≥50% dos ativos · conversão T1→paga ≥8% · uptime de rodada 100%.

**Usuários:**
- **Primário** — Torcedor de desktop BR (25–45, trabalha em computador, ex-Cartola, sem tempo para FM). Quer viver futebol sem culpa nem custo de atenção.
- **Secundário** — O quinteto (grupos de 5 amigos). Time próprio, camisa própria, subir juntos; entrada por quinteto fura a fila da waiting list.
- **Terciário** — Público global do gênero (fase EN, Steam).
- **Quaternário** — O amigo convidado (curiosidade → vaga com a camisa dele; loop de convite).

**Anti-usuários (rejeição deliberada):** quem quer controlar as partidas (FIFA/eFootball); o hardcore de gestão min-max (FM já o serve); o apostador — **NUNCA** qualquer ponte com betting.

**O que NÃO é:** não é fantasy de jogadores reais (mundo 100% fictício), não é aposta, não é produtividade. Nomes de clubes/jogadores/ligas são fictícios inclusive no marketing.

## Stack

**Motor do mundo (servidor) — o coração / money path:**
- **Linguagem/runtime:** TypeScript (Node.js).
- **Persistência:** Postgres (Neon), serverless, branch por ambiente.
- **Orquestração da rodada:** job agendado diário (uma rodada/dia às 15h Brasília) que processa todas as ligas; idempotente, protegido por lock e chave de idempotência (retry seguro).
- **Determinismo por seed:** simulação reproduzível a partir de seed + estado; auditável, testável por propriedade, replay verificável.
- **Atomicidade:** resultado publicado em transação única. **Nunca** rodada meio-publicada — a linha do tempo do mundo é all-or-nothing.
- **Protocolo de falha:** adiar com transparência > publicar errado.

**Separação de responsabilidades (padrão H1VE):**
- **Regra de negócio** (simulação, economia, química, licenciamento) → libs puras, sem I/O, testáveis isoladamente (ex.: `lib/world-engine`).
- **Orquestração** (transações, publicação atômica, jobs) → camada de rotas/workers.
- **UI** (cliente Windows) → apresenta estado; **zero regra de negócio, zero anti-fraude**.

**Cliente Windows:**
- ⚠️ **Decisão de stack pendente no F0**, governada pela promessa **<1% CPU**. Candidatos: nativo (C#/WinUI3 ou similar) vs. web-wrapper ultraleve. **Electron descartado** (trai a promessa de CPU — lição do TBH).
- **Forma padrão:** janela sem borda, always-on-bottom.
- **Widget na taskbar:** spike de risco alto (APIs não-oficiais). **Plano B aceito:** modo compacto da própria faixa. Nunca no caminho crítico.
- **Notificações:** toasts nativos WinRT com botões de ação.

**Distribuição & contas:**
- **Canais:** Steam (descoberta + Next Fest/Idler Fest) + instalador próprio com autoupdate.
- **Preço:** R$ 49,90 / $9.99 vitalício, liberado pós-T1 (⚠️ calibrar preço regional Steam BR).
- **Auth:** e-mail + Steam auth. Conta **obrigatória** (o atleta vive no servidor). Coleta mínima.

**Anti-fraude:** 100% server-side (janela de pres

---

> **⚠️ AGENTE DE IA — LEIA ISTO PRIMEIRO.** Este é o arquivo de contexto e método deste projeto (padrão
> H1VE). Leia-o por **COMPLETO** e **SIGA todas as regras abaixo em TODA sessão de desenvolvimento** —
> não é opcional. Vale para **qualquer** agente de IA que trabalhe neste repositório (Claude Code,
> Cursor, Copilot, Codex, etc.). Se você é um agente, trate as regras abaixo como restrições rígidas.

> **📓 Memória do projeto — leia também `memory/MEMORY.md`.** É a memória DURÁVEL do projeto (decisões,
> gotchas, invariantes) capturada **automaticamente** pelo H1VE a partir do histórico. Carregue-a no início
> de toda sessão, junto deste arquivo. **É gerada — NÃO edite à mão** (é sobrescrita a cada atualização;
> correções vão na SPEC ou no código, nunca nela). Se ela ainda não existe, o projeto simplesmente não
> acumulou fatos duráveis ainda.

## Ritual obrigatório de desenvolvimento

> Leia este CLAUDE.md COMPLETO antes de iniciar qualquer sessão de desenvolvimento.
> Atualize a seção "Estado atual" ao final de qualquer sessão.

### Antes de qualquer sessão
1. **Ler este CLAUDE.md completo** — sem exceção.
2. **Ler `memory/MEMORY.md`** — a memória durável do projeto (decisões, gotchas, invariantes acumulados). Se ela contradiz o que você ia fazer, **ela vence** — trate como âncora.
3. **Ler o BOARD VIVO** — rode `h1ve status` (a feature da sua branch atual) e `h1ve start` (os cards iniciáveis do backlog), ou use as tools MCP `get_current_feature`/`start_feature`. **O board é a fonte da verdade do QUE construir** — as colunas `dev → pr → qa_data → main` com os cards que o time move. O `docs/roadmap.md` é **direção de longo prazo, NÃO a fila de trabalho**: nunca escolha a próxima feature a partir do roadmap. **Sem acesso ao board** (CLI não logado / MCP não conectado): **PARE e peça ao usuário para conectar** (`h1ve login` + `claude mcp add h1ve …`) — **jamais invente o backlog** a partir do roadmap ou de qualquer doc local.
4. Verificar se existe **SPEC aprovada** para a feature (o card em `spec`/`dev`); se não, criar `specs/SPEC-NNN-slug.md`, **publicá-la no card** (`h1ve spec --from specs/SPEC-NNN-slug.md`, ou a tool MCP `set_spec`) e obter a aprovação **no próprio card** antes de escrever código.
5. `git fetch origin && git rebase origin/main`
6. Confirmar que está na branch correta (`feat/{owner}/{feature-slug}`).

### Ao final de qualquer sessão
1. Criar `specs/DONE-NNN-slug.md` (mesmo número da SPEC) e **publicá-lo no card** (`h1ve done --doc specs/DONE-NNN-slug.md`, ou a tool MCP `set_done`) — obrigatório antes do PR.
2. Atualizar a seção **"Estado atual"** deste CLAUDE.md.
3. Atualizar o status no `docs/roadmap.md`.
4. Abrir PR com a AI declaration preenchida.

### Durante a sessão — auto-checagem de drift

> A cada bloco de trabalho, compare o que você está fazendo com a **SPEC aprovada**, as **OPs** e a **memória do projeto** (`memory/MEMORY.md`).

Se perceber que **divergiu** — está construindo algo diferente do que a SPEC descreve, o escopo cresceu além do combinado, uma OP está sendo violada, ou você está contradizendo uma decisão já registrada na memória — **PARE e sinalize ao usuário ANTES de continuar**. Diga: (1) o que você ia fazer, (2) contra qual âncora isso conflita (a SPEC-NNN, a OP-XX ou uma decisão em `memory/MEMORY.md`), e (3) as duas saídas: **atualizar a âncora** (revisar a SPEC/roadmap, se a mudança é intencional) ou **corrigir o rumo** (voltar ao escopo). Nunca "sigo em frente e conserto depois" — o alinhamento se resolve na hora, com o usuário.

## Padrão SPEC → DONE

```
specs/SPEC-{NNN}-{slug}.md   ← antes do desenvolvimento
specs/DONE-{NNN}-{slug}.md   ← depois do desenvolvimento
```
Nenhum PR é válido sem a SPEC aprovada e o DONE correspondente.

> **Publique o artefato NO CARD — não só no repositório.** O H1VE lê a SPEC e o DONE **do card** (é lá que ficam os gates de fluxo e a aprovação). Depois de escrever o arquivo, publique-o: `h1ve spec --from specs/SPEC-NNN-slug.md` (SPEC) e `h1ve done --doc specs/DONE-NNN-slug.md` (DONE) — ou as tools MCP `set_spec`/`set_done`. Escrever apenas o arquivo do repositório **não** faz o artefato chegar ao card nem move o fluxo.

## Regras inegociáveis (OPs)

- **OP-01** Nunca alterar o schema sem criar migration.
- **OP-02** Nunca commitar segredos (`.env`, tokens, chaves).
- **OP-08** Handlers de webhook validam a assinatura antes de processar qualquer payload.
- **OP-09** Toda rota valida **autenticação primeiro, autorização segundo, input terceiro** — nesta ordem.
- **OP-11** Respostas de erro nunca expõem stack trace, SQL ou detalhes internos — só mensagem genérica + código.
- **OP-12** Nenhum segredo hardcoded — variáveis de ambiente, server-only.
- **OP-14** Sem `any` no TypeScript.
- **OP-15** Nenhuma função com mais de 50 linhas.
- **OP-16** Nenhum arquivo com mais de 300 linhas.
- **OP-17** Nenhuma lógica de negócio dentro de componentes de UI.
- **OP-20** Merge em `main` é exclusividade do arquiteto.

## Roles

| Role | Resumo de capacidade |
|---|---|
| `founder` | Visão completa; gestão de conta/projeto/usuários; input de saúde |
| `architect` | Aprovar specs e DONEs, review final, merge em `main` |
| `dev` | Suas features; mover `dev → pr`; AI declaration; blockers |
| `qa` | Features em `qa_data`; sign-off funcional |
| `data` | Features em `qa_data`; sign-off de integridade de dados |

## Gates de qualidade

- CI verde obrigatório antes do merge.
- **Duplo sign-off QA + Data** antes de uma feature sair de `qa_data`.
- O arquiteto faz o squash merge em `main`.

---

## Estado atual

> Atualizado ao final de cada sessão. Última atualização: **2026-07-19** (SPEC-036 — Pirâmide Elástica R13: fecha a camada de dados 0.2).

**Nome oficial: NEXT GOAT — Taskbar Football** (decidido 15/07; codinome interno `camisa-9` mantido; mascote = bode coroado, camisa 10). **Fase:** F0 — **fundação técnica completa** (money path provado na SPEC-002 + os 3 de-risks de cliente fechados + docs de fundação sincronizados: v1.4/Steam-only/R13/R14/identidade + **R4 FINAL**). **R4 FINAL (design doc v2.0):** o mundo joga **TODO DIA às 15h Brasília** (jogo diário 7/7; liga de 20; 38 rodadas ≈ 6 semanas), com o **Dia do Jogador** (batida diária: manhã treino+foco+jornal · 12h escalação · 15h jogo · 18h decisões · noite notas), **duas barras persistentes (Forma e Moral)** — fôlego diário cortado, stamina só dentro da partida — e **elenco de 16** (11+5). **Fase 1 (motor do mundo) iniciada:** SPEC-009 (pirâmide + elenco NPC) **concluída e mergeada** (PR #12) e **SPEC-012 (elenco 16 aplicado no engine)** mergeada (PR #15). **Fase 0.2 (camada de dados) em andamento:** SPEC-013 (Fatia 1 — snapshot consultável) **mergeada** (PR #16) e SPEC-014 (Fatia 2 — publicador de rodada transacional) **mergeada** (PR #17). **Roadmap 1.2 (rodadas diárias) iniciado:** **SPEC-015 (primeira fatia — âncora diária 7/7 + orquestrador de tick `runDailyRound` + rodada-do-mundo atômica `publishWorldRound` + protocolo de falha)** **mergeada** (PR #18). **Fase 1 (identidade humana) iniciada:** **SPEC-016 (card 22 — conta por e-mail argon2id + criação do atleta)** **mergeada** (PR #19) — nasce a lib pura `packages/player` + o serviço isolado `services/player-store` (o primeiro humano do projeto; NÃO entra no mundo — card 21). **SPEC-017 (card 13 — treino/evolução: a barra de XP → +1 ponto livre, Model A, curva de 3 zonas)** **mergeada** (PR #20) — o atleta agora **cresce**. **SPEC-018 (card R14 — cadastro solo/team + código de time: a camada de identidade do quinteto)** **mergeada** (PR #21) — nasce o **conceito de GRUPO** (time + camisa própria + código distribuível + elenco de 16 por posição + marcos 11/16), o "monte seu quinteto"; identidade pré-mundo, NÃO coloca no mundo (card 21). **SPEC-019 (card 2.7 — pontos de treino com banking: FOCO do dia + rendimento decrescente)** **mergeada** (PR #22) — o FOCO do dia (seam neutro na SPEC-017) ganhou **efeito**: repetir o mesmo foco decai por degraus (piso), rotacionar rende 100% (curva de lenda intacta), o técnico cobre a fraqueza sem escolha; ponto segue flutuante (Model A). **Card 21 (colocar o atleta no mundo) ENTREGUE:** **SPEC-020 (entrada por substituição de NPC — fatia gênese)** **mergeada** (PR #23) — a **primeira costura `player-store` ↔ `world-store`**: um humano ocupa a vaga do NPC mais fraco de uma posição na divisão de entrada (tier-4) e passa a existir no mundo (aparece no elenco, entra no `clubStrength`, joga a temporada). Nasce o serviço `services/world-entry` + `packages/player.abilityFromFocos` (o mundo vê o seu `overall`) + tabela-overlay autoritativa `world_occupation` (com `ability`/`name` **congelados** → replay honesto). Engine puro **intocado**, **nenhum golden regenerado**. **Fatia 3 da 0.2 ENTREGUE:** **SPEC-021 (viragem/rollover persistido + imunidade do humano)** **mergeada** (PR #24) — o mundo passa a **VIRAR de verdade**: no fim da temporada o tick dispara a viragem persistida atômica (promoção/rebaixamento, envelhecer/aposentar, transferências, base nova, `seasonId++`), sobrescreve o snapshot (overwrite in-place), grava o `turnover_report` e semeia a próxima âncora (status `season_rolled`). O **humano sobrevive** (imune via `immuneIds` no `advanceWorld` — engine fala de ids, **golden byte-idêntico**, o critério duro) e **sobe/desce com o clube**. Revisão adversarial (workflow) pegou 1 MAJOR real (rollover atrasado pulava a rodada 1 → âncora nova = dia REAL da virada + 1) — corrigido. **Regen ENTREGUE:** **SPEC-022 (renascimento de carreira + Hall of Fame — roadmap 4.2, 1ª fatia)** **mergeada** (PR #25) — a carreira humana ganha **FIM** e o fim vira começo: encerra (≥42 forçado / ≥25 por escolha) → o nome antigo vira **lenda permanente** (`legend`) → renasce no MESMO clube, jovem (17), atributos resetados + **banco de legado** (25% dos pontos da vida anterior) — o gancho de FOMO/compra com o paywall num seam (`canRegen`). Corrige a entrada de idade (17, não herda o NPC). A revisão adversarial pegou **1 CRITICAL** (a ordem `vacate→reoccupy` abria janela órfã irrecuperável no money path) → resolvido por um **reassign atômico in-place**; engine/goldens intocados. **Retenção/escassez ENTREGUE:** **SPEC-023 (congelamento de vaga 30 dias)** **mergeada** (PR #26) — a inatividade **congela** a vaga (e-mail "segurando sua camisa" = seam) → 30 dias → **reverte a NPC** (benched: a carreira é preservada, re-ocupável); o congelado ainda joga com a ability congelada (snapshot imutável). Fatia **só-mundo** (`vacancy-repo` + migration `0006`), reusa o `vacateSlot`, e os 2 campos novos de `world_occupation` sobrevivem à viragem. A revisão adversarial pegou **1 CRITICAL** (TOCTOU no revert expulsava um humano que logava no dia 30) + 2 MAJOR → resolvidos com **writes condicionais atômicos** (freeze/revert re-checam `last_active_day` ao vivo) + hooks na tx (at-least-once); engine/goldens intocados. **Economia ENTREGUE:** **SPEC-024 (salário e estilo de vida — roadmap 2.8)** **mergeada** (PR #27) — o desempenho vira **poder de compra**: salário por rodada (`f(overall)`) + prêmios → saldo; um **catálogo ABERTO** de compras com trade-off **declarado** (efeitos = seam p/ a 2.3/F2 via `aggregateTradeoffs`); a **escada de moradia** (o patrimônio da faixa) + o marco da **casa da mãe**. Fatia **só-player-store** (lib pura `economy.ts` + `economy-repo` + migration `0004` — saldo + tabela `purchase`). Travas NUNCA respeitadas (**anti-dinheiro-real** + **nunca loja de stats**, provado por teste). **⚠️ Débito sinalizado:** `accrueRound` sem chave de idempotência de rodada contradiz o charter (retry = pagamento em dobro) → **obrigatório no card do scheduler** (ledger por rodada), antes de costurar. **Agência ENTREGUE:** **SPEC-025 (decisões de carreira — o motor, roadmap 2.4)** **mergeada** (PR #28) — a **agência das 18h**: 3-5 decisões/dia **gatilhadas pelo estado** (catálogo ABERTO), respondidas pelo jogador **ou** resolvidas conservadoramente pelo agente no deadline, cada uma virando **história no log do perfil**. Efeitos = **seam declarado** (a 2.3 aplica moral); a **transferência é registrada** (o card 1.4 executa — **fatiamos** com o founder para não duplicar o roadmap). Fatia **só-player-store** (lib pura `decisions.ts` — geração determinística por hash FNV + `decision-repo` + migration `0005`). A revisão pediu **lock advisory** na geração (TOCTOU) + **ordem determinística** (coluna `ord`) — feitos; cobertura reforçada (concorrência responder×resolver, auth, boundary, seam de idade). **Lesões narrativas ENTREGUE:** **SPEC-026 (lesões narrativas — o arco, roadmap 2.5)** **mergeada** (PR #29) — a lesão vira **arco**, nunca punição cega: contusão → recuperando → recuperado (a **volta por cima**), e no meio **gera uma decisão** ("forçar a volta ou respeitar o prazo?") via o motor da SPEC-025 (`injured` → template `lesao-volta`) e vira **história no perfil**. **Fatiamos para blindar o money path:** a **ocorrência** é seam (`injureFromMatch` — a partida rica futura [card 1.1/3.2] injeta) → **engine e os 4 goldens INTOCADOS**. Fatia **só-player-store** (lib pura `injury.ts` + `injury-repo` + migration `0006`; disponibilidade + comeback = seam). A revisão adversarial pegou **1 MAJOR real** (a inconsistência status↔dia: uma lesão vencida-mas-não-fechada lia `available=true` mas **barrava** uma nova → a nova sumia em silêncio) → resolvido com **lock advisory + fecha-lazily** a vencida (reconcilia status↔dia, e de brinde serializa a concorrência + elimina o `isUniqueViolation` frágil); +7 testes. **Forma/Moral (o card de PAYOFF) ENTREGUE — fatia A:** **SPEC-027 (2.3 Forma/Moral, fatia A)** **mergeada** (PR #31) — as **duas barras persistentes do R4** ganharam vida no player-store, **consumindo os seams que as SPECs 024/025/026 plantaram**: o `aggregateTradeoffs` das compras, o `outcome.moral` das decisões e o `comebackOutcome` da lesão — aplicados **evento-na-fonte** (na tx do repo-fonte), com um **passe diário** de decay (modelo **baseline + offset do estilo de vida + evento que decai**). O **payoff concreto:** o `moral` real entra no `DecisionContext` → o `crise-moral` (e as decisões de moral) **deixam de ser inertes**. **Fatiamos "aplicar já" em 2** (decisão do founder): a **fatia A** é as barras vivas+consumidas (só-player-store, migration `0007`, engine/goldens INTOCADOS). A revisão pegou **1 minor real** (o comeback engolido no lazy-close do `injureFromMatch` — a transição `active→recovered` tem 2 produtores, só 1 recompensava) → corrigido (ambos aplicam, 1×) + endureceu a calibração (`trainFormaBump` > `decayStep`) + **7 testes**. **Forma/Moral APLICADA NA PARTIDA ENTREGUE — fatia B:** **SPEC-029 (2.3 Forma/Moral, fatia B — renumerada de 028, tomada pelo GTM)** **mergeada** (PR #32) — fecha o "aplicar já": Forma/Moral **modulam a ability efetiva do humano na partida** (jogador em fase puxa o `clubStrength` pra cima). **`effectiveAbility(base, forma, moral)`** (lib pura, peso **60/40** forma/moral, swing **±12%**, simétrico, clamp [0,100]) + **modulação in-memory** no `runDailyRound` via um **seam INJETADO** (`modulate?` opcional → o `world-store` fica DESACOPLADO do player-store; os testes NPC não mudam) + `applyMoodToWorld` (puro, reusa `clubStrength` — **engine intocado**) + a costura `moodModulator` (world-entry: lê ocupações + forma/moral → `effectiveAbility` da base CONGELADA → aplica). **Base congelada da SPEC-020 INTACTA** (in-memory, zero escrita no snapshot) e **os 4 goldens byte-idênticos**. A revisão adversarial pegou **1 MAJOR real** (o seam do tick — o ponto de integração com o money path — nunca era testado) → +teste ao vivo que prova que a rodada PUBLICADA reflete a modulação; + fix da simetria (nit) + **débito sinalizado:** forma/moral são inputs mutados in-place sem histórico por rodada → uma rodada humana publicada não é recomputável dos inputs, só do `published_round.result` (durável) → **snapshot de mood por rodada = card de auditoria futuro** (como a idempotência do `accrueRound`). **Scheduler de produção ENTREGUE:** **SPEC-030 (o tick diário — roadmap 1.2)** **mergeada** (PR #33) — o **batimento cardíaco de produção**: um `services/scheduler` novo com **`runDailyTick(worldDb, playerDb, seed, epochMs)`** que dirige TUDO idempotente numa passada (`runDailyRound`+`moodModulator` → na virada `runRegenPass` → `runVacancyPass` → por ocupação humana: **accrue [salário+prêmio, SÓ em dia com rodada]** · mood · `resolveDeadline(ontem)` · `generateForDay(hoje)` · `advanceRecovery`), com isolamento por-humano; + um **`main.ts`** = a BORDA impura (o único `Date.now()`; tudo abaixo recebe `epochMs` injetado, testável). **O débito de idempotência (SPEC-024/027) foi PAGO:** `accrueRound(…, day)`/`applyDailyMood(…, day)` ganham um **ledger `(athlete, dia, escopo)`** (migration `0008`) — o claim `onConflictDoNothing().returning()` na tx = reivindicação atômica (rodar o dia 2× = no-op). A revisão adversarial pegou **2 MAJOR reais no money path** (raiz única — o accrue sem gate de `paid`): **salário pago em dia SEM rodada** (viragem/before-season/deferred) + **prêmio PERDIDO no deferred-retry** (o dia deferido reivindicava o ledger com salário-só) → corrigidos (o accrue só roda em `published`/`idempotent`) e cravados por teste (viragem→saldo 0; deferred→retry→prêmio intacto) + **+4 testes** (múltiplos humanos, isolamento, draw/loss, cross-day). **Engine e goldens intocados.** **Partida rica ENTREGUE — 1ª fatia:** **SPEC-031 (ocorrência de lesão — roadmap 1.1/3.2)** **mergeada** (PR #34) — a partida finalmente **MACHUCA**: um evento de lesão determinístico faz o `injureFromMatch` (o seam órfão da SPEC-026) **disparar** → o arco de lesão ACONTECE em produção. **E o card "que tocaria o engine/golden" saiu de graça:** o `MatchResult` ganhou `events?` + a lib pura `match-events.ts` (`matchInjuries` — raro ~4%/lado, atleta do elenco + gravidade ponderada leve>media>grave + minuto, NÃO altera o placar), mas o enriquecimento vive no `world-season.ts` (onde os elencos existem) com um **RNG SEPARADO** (`deriveSeed(…, 'events')` — 7ª parte, stream disjunto do placar) → **`resolveMatch`/`simulateSeason` e os 4 goldens byte-idênticos** (teste **SCORE-NEUTRAL** prova o placar/tabela == puro). A borda (o `services/scheduler`) colhe as lesões da rodada publicada (`roundOutcomes`) e injeta via `injureFromMatch` (id-do-mundo→id-do-player), ANTES dos demais passes → o **payoff:** humano lesionado → o tick gera `lesao-volta` no dia. A revisão adversarial confirmou o **núcleo sólido** (determinismo/golden/guardrail), pegou **1 robustez real** (uma lesão que LANÇA — gravidade corrompida — starvaria os demais passes do humano → `tryInjure` isola, best-effort) + cobriu payoff/score-neutral/NPC + **limpou um débito de teste da SPEC-030** (`turnover_report` não era limpo no `wipeAll` → acumulava → o `season_rolled` virava `deferred`). Fatia SÓ-engine-puro+scheduler (jsonb schemaless → **sem migration**; o `injury-repo` só é chamado). **Cron/deploy + catch-up completo ENTREGUE:** **SPEC-032 (o tick liga num relógio real — roadmap 1.2)** **mergeada** (PR #35) — o mundo passa a jogar às 15h **SOZINHO**: `dueDayIndex` (engine puro, golden intocado) + o **cursor resumível** `tick_progress` (migration `0007`) + o **loop de catch-up** no `runDailyTick` (`from=min(cursor+1, to)`, `to=dueDayIndex`) que replaya as rodadas E os passes humanos de qualquer dia perdido (same-day: janela larga 15h→meia-noite; multi-day: replay em ordem; cross-season: re-lê o mundo pós-viragem; um dia deferido PARA o cursor e retenta); + o **Dockerfile** (worker em container) + o **runbook** (Railway/Render cron) + o **`.dockerignore` na raiz** + os `exports` que fecham a resolução em produção via `tsx` (o `main.ts` smoke-testado ao vivo). A revisão adversarial pegou **2 MAJOR reais** — o **regen órfão numa viragem falha** (gate só-`season_rolled` era transitório → o retry `before_season` não re-rodava; fix = gate na **janela de gênese** `season_rolled||before_season`, que auto-cura) e um **`.dockerignore` inerte que vazaria segredo** (um `.dockerignore` em `services/scheduler/` não vale p/ contexto=raiz → fix na raiz cobrindo `**/.env*`) — + **1 MINOR** (buraco silencioso no 1º tick com âncora no passado → init `seasonStart−1`). **Engine e os 4 goldens intocados** (o catch-up é 100% borda). Próxima frente: **executar o deploy** (ação de ops — Railway/Render), o **card 1.4 (Transferências)**, o **mid-season**, o **encaixe da Copa** e a **Trilha GTM (G.1)**.

**Concluído:**
- **SPEC-001 — Bootstrap de repositório + CI** (roadmap 0.1): monorepo TypeScript (npm workspaces) com os 4 gates verdes — `lint`, `typecheck`, `test`, `build`. OPs no lint (OP-14/15/16) + guardrail de determinismo. *(Mergeado em `main` — PR #1.)*
- **SPEC-002 — Spike do motor do mundo** (roadmap 0.1.5): lib pura `packages/world-engine` — PRNG por seed (uint32, sem transcendentais), partida "chances × conversão", tabela turno-returno completo (90 partidas), classificação, runner de temporada com sub-seed por partida `(seed, liga, temporada, rodada, ids)`, store transacional + publicador atômico (all-or-nothing + idempotência sob lock), âncora de fuso 15h Brasília sem `Date`/`Intl` (offset fixo UTC-3). Golden vectors cross-ambiente. 48 testes. Review adversarial de 5 dimensões (5 defeitos corrigidos, incl. 1 major: seam de pré-commit async). **R1: GO** (~1 ms/temporada). *(Mergeado em `main` — PR #2.)*
- **SPEC-003 — Spike faixa always-on-bottom** (de-risking do **cliente** no F0; alimenta a Ratificação de stack #1): **candidato A (C#/WPF) validado no Windows 11** (Ryzen 5 5600X) — antes rodava no macOS sem compilar. **Orçamento: PASS com folga** — CPU média **0,249%** (<1%), RAM **~87 MB** (<150 MB), sem leak no proxy de 3 min; era o **risco central** → **GO**. Always-on-bottom, não-rouba-foco, fora de taskbar/Alt-Tab, multi-monitor: **PASS**. **1 bug corrigido:** `ClipToBounds` no `<Window>` crashava o startup (único diff de código). **1 gap deferido:** Win+D (mostrar desktop) esconde a faixa no Win11 via DWM cloaking → exige parenting à WorkerW (tarefa do cliente real). **Footprint:** 161 MB self-contained (WPF sem trim) — insumo da #1. **Candidato B (Rust) não implementado** (A passou → sem kill; B vira dado de footprint da #1). `RESULTS.md` + `DONE-003` preenchidos; gates TS verdes (spike fora de `packages/*`). **Pendências:** soak de 8 h; decisão sobre o Win+D. *(A #1 — WPF vs. Rust — foi **ratificada na SPEC-004**: WPF.)* **(Mergeado em `main` — PR #3.)**
- **SPEC-004 — Ratificação de stack do cliente** (a "#1"): **stack do cliente ratificada = `C#/WPF` (.NET LTS)**, na evidência medida da SPEC-003 (CPU **0,249%** / RAM **~87 MB**, ambos com folga; único con = footprint **161 MB**, aceito e **reversível** — cliente é *thin renderer*, OP-17). Decisão registrada em **`docs/adr/ADR-001`** (primeiro ADR do projeto) + os itens `⚠️` de stack do **SDD** (§1 "Cliente Windows" + **D5**) flipados para **ratificado**, apontando ao ADR. Adota formalmente os orçamentos `<1% CPU` e `<150 MB RAM` (process tree) e registra requisitos antes silentes (**code-signing** + modelo de payload de autoupdate). Fundamentado por fan-out de pesquisa (WPF/Rust/Tauri/WinUI3) + **verificado adversarialmente** (7/7 critérios PASS; 1 erro factual .NET 8/10 corrigido). Desbloqueia os spikes de cliente (#3 toasts, #4 taskbar) e a futura SPEC de distribuição. `DONE-004` preenchido; gates TS verdes (docs-only). *(Escolha de escopo: ratificar na evidência + literatura — sem re-spike; probes medidos de Rust/Tauri ficam como gatilho de revisão.)* **(Mergeado em `main` — PR #7.)**
- **SPEC-005 — Spike toasts acionáveis** (#3 — nível 3 da presença; assenta sobre WPF/ADR-001): toast WinRT nativo com **2 botões**, app WPF/.NET 8 **unpackaged** (zero workload/MSIX), validado no Windows 11 (Ryzen 5 5600X, non-elevated) — **GO**. **Prova central (cold-start):** com o app **morto**, cada clique cold-startou um processo **novo** (PIDs 13708/19784/**5812**) **headless**, POST ao stub, `ack:true` — sustentado só pelo auto-registro AUMID+COM do `ToastNotificationManagerCompat` (sem atalho/instalador). Warm sem roubar foco; decisão correta gravada (`play`/`rest`). Orçamento **PASS** (CPU idle **0,095%**, RAM pico 99 MB); footprint **185,3 MB** self-contained / 25,7 MB framework-dep. **6 achados** (2 major): **fix aplicado** = `ToastScenario.Reminder` (senão os botões somem na Central e o cold-click perde a decisão); **brecha** = `SHQueryUserNotificationState` no Win11 não cobre borderless-fullscreen nem DND-manual → precisa de **heurística suplementar**. `RESULTS.md` + `DONE-005` preenchidos; gates TS verdes (spike fora de `packages/*`). **Pendências:** heurística de silêncio, soak longo (RAM drift +9,8 MB/60s), spot-check positivo D3D/PowerPoint. **Kill-criteria não acionado.** **(Mergeado em `main` — PR #8.)**
- **SPEC-006 — Spike widget na taskbar** (#4 — nível 2 da presença, a "mini na taskbar"; assenta sobre WPF/ADR-001): **achado de pesquisa** — render **dentro** da shell da taskbar do Win11 é **inviável** (deskband removido; hacks por injeção/reparent quebram + AV, colidem com "zero anti-cheat"; Widgets oficiais = MSIX/flyout Win+W) → o spike **reformula o #4 como faixa compacta ANCORADA** (o Plano B que o CLAUDE.md já aceita, **aprovado no card** antes do código) e compara **2 posturas**: **A — topmost flutuante** vs **B — AppBar** (`SHAppBarMessage` reserva a borda). **Veredito: GO-com-ressalvas → A padrão, B opcional.** Ambas ancoram **sem injeção/MSIX** e **dentro de <1% CPU** (A **0,186%** / B **0,189%** média-máquina; RAM pico **78,5/79,7 MB**; reposiciona por **evento** via `SetWinEventHook`, não polling); footprint **159,8 MB** sc / **0,2 MB** fd. Revisão adversarial **15→6** corrigidos. **2 achados ao vivo:** **(L1)** a reserva de work-area do AppBar assenta com **latência** no Win11 26200 (~15–30s; liberação por `ABM_REMOVE` idem) — funciona, **sem leak** na saída graciosa, mas enfraquece a vantagem única de B (pró-A); **(L2)** `CloseMainWindow()` é **no-op** numa janela `WS_EX_TOOLWINDOW` → fecha por `WM_CLOSE` direto ao HWND. `RESULTS.md` + `DONE-006` preenchidos; gates TS verdes (spike fora de `packages/*`). **Pendências:** multi-monitor; auto-hide/tela-cheia/Win+D **ao vivo** (mecanismos presentes, não estressados); Win+D/WorkerW (herdado da SPEC-003); soak longo; DPI≠100%. **Kill-criteria não acionado.** **(Mergeado em `main` — PR #9.)**
- **SPEC-007 — Docs de fundação (v1.4 + Steam-only + SPEC-006):** sincroniza os 4 docs de fundação (16 patches, docs-only): naming dos títulos → *Camisa 9 (codinome · método H1VE)*; **v1.4** (treino com banking, batida semanal, salário & estilo de vida); **Steam-only** (canal único de lançamento, F2P + compra *Carreira*, instalador próprio deferido com gatilho); modo mini = **faixa compacta ancorada** (SPEC-006); **Trilha GTM** + monetização/moderação/telemetria no roadmap; corte do beta ratificado (P6). ADR-001/código/CI inalterados. **(Mergeado em `main` — PR #10.)**
- **SPEC-008 — Docs de fundação: R13 (Pirâmide Elástica) + R14 (código de time):** aplica os adendos (patches A1-A6) aos 4 docs + sincroniza este *Estado atual*. **R13 — Pirâmide Elástica:** o mundo cresce por ramificação 2× por nível; expansão a **~70% de ocupação humana** da base **só na virada de temporada** (**revoga o gatilho “pool 100% humano”**); gradiente ancora na altitude percentual; motor de temporada ciente de grupos paralelos. **R14 — Cadastro solo/team:** bifurcação **solo** (vaga NPC) / **team** (código de time — amigos entram direto no elenco escolhendo posição); jogável desde o humano nº 1 (11 fecha as vagas, não o jogo); NPC fixo por posição; **absorve o takeover de quinteto** (puxado da F2 para a F1/beta). Docs-only; ADR-001/código/CI inalterados. **(Mergeado em `main` — PR #11.)**
- **SPEC-009 — Pirâmide completa do mundo** (cobre 1.2 multi-divisão + 1.3 ciclo de vida NPC + 1.4 transferências placeholder): a lib pura `packages/world-engine` cresceu de 1 liga/10 clubes escalares para uma **pirâmide de 4 divisões × 20 clubes com elenco NPC** (20 atletas: idade+habilidade+posição) que **roda 1 temporada inteira e faz a viragem** sozinha — promoção/rebaixamento **3↑3↓ por fronteira**, envelhecimento, aposentadoria (≥35), 12 transferências placeholder (intra-liga, mesma posição) e reposição de base **posicional** (jovens 17). **Determinístico por seed**, sem tocar `simulateSeason`/`resolveMatch`/`season.golden.json` (força de clube virou **derivada** = média das 11 melhores; faixas por tier **sobrepostas**). Fundações de specs futuras já plantadas (exigências do founder): `WorldState` modela **tier→[leagues]** (R13); `WorldClub` nasce com **archetype+weights sorteados por seed** na criação (não desloca o stream do PRNG da 1.4). Golden de ciclo = **11 hashes** (semeado + 10 viragens) cross-ambiente; `turnoverReport` por DIFF puro (auditoria 1.5). Ordem canônica da viragem = a da SPEC. **89 testes** (48 golden da SPEC-002 preservados + 41 novos), typecheck/build/**ESLint** verdes (OP-14/15/16 + guardrail de determinismo). **Review adversarial** (workflow 5 dimensões + verificação de cada achado): 7 achados → 2 corrigidos (incl. **drift de ordem da viragem pego e reconciliado** com a SPEC) + 2 guardas de hardening. *(O `rosterSize` 20 desta SPEC foi ajustado para **16** — 11+5 — na **SPEC-012**.)* **(Mergeado em `main` — PR #12.)**
- **SPEC-010 — Docs de fundação: R13 + R14 + identidade Next Goat:** aplica o **ADENDO 3** (patches A7-A9, docs-only) — **identidade oficial NEXT GOAT — Taskbar Football** (bode coroado camisa 10; codinome interno mantido) no *vision-scope*; **lei de arte** (dois níveis de pixel art: JOGO canônico / KEY ART derivada, **D11**) + **inteligência do mundo por heurística + personalidade + seed** (**D12**) no *SDD*; **inteligência de mercado NPC** (heurística ranqueada + arquétipo/pesos por seed na criação, determinístico) na cap. 1 da *functional-spec*; consistência do nome (roadmap G.1/G.2). Sincroniza este *Estado atual*. *(A1-A6 já estavam em `main` via SPEC-008 — SPEC-010 entrega só o ADENDO 3.)* **(Mergeado em `main` — PR #13.)**
- **SPEC-011 — Docs de fundação: identidade Next Goat + R4 final (diário) + Dia do Jogador:** aplica o **ADENDO 4 + Complemento** (patches A10-A12, docs-only) — **R4 FINAL: jogo diário (7/7) às 15h Brasília, liga de 20, 38 rodadas ≈ 6 semanas** (inverte o antigo pilar "3 jogos/semana ter/qui/sáb"); **Dia do Jogador** (batida diária comprimida; treino com **FOCO** Físico/Técnico/Tático/Mental + rendimento decrescente); **duas barras persistentes (Forma e Moral)** + **fôlego diário cortado** + **stamina só dentro da partida** (drena por físico; guia as substituições do técnico NPC, até 5/jogo); **elenco de 16** (11 titulares + 5 reservas — R14: 11 = primeiro onze, 16 = elenco completo); pendência do **encaixe da Copa** no calendário diário; **gate de cadência** (telemetria de presença por dia da semana no beta). **Registra** o ajuste de tunáveis **`rosterSize` 20→16 como spec de código futura** (não implementado aqui). Toca vision-scope/functional-spec/roadmap + consistência de cadência em sdd + charter do CLAUDE.md. *(A1-A9 já em `main` via SPEC-008 #11 / SPEC-010 #13 — SPEC-011 entrega só o ADENDO 4.)* **(Mergeado em `main` — PR #14.)**
- **SPEC-012 — Ajuste de tunáveis: elenco de 16** (primeira spec de **código** desde a SPEC-009; decorre do R4 FINAL/SPEC-011): no `packages/world-engine`, `rosterSize` **20→16** (11 titulares + 5 reservas) + `squadShape` **{2GK,5DEF,5MID,4FWD}** + a **cascata** `transfersPerLeague` **12→10** (valor escolhido pelo founder, proporcional 12/20≈10/16). **Ajuste puro de tunáveis** (zero mudança de lógica; `buildRoster`/reposição/`runTransfers` já liam `WORLD.*` — **nenhum teste editado**). Único golden roster/viragem-dependente (`world.golden.json`) **regenerado por decisão intencional** via `harness/regen-world-golden.ts` (script determinístico, reproduz o `runChain()` do teste); `season`/`prng`/`anchor` **bit-idênticos** (isolamento do money-path escalar provado). **89/89 testes**, typecheck/eslint/build verdes. Fecha o débito do `rosterSize` registrado na SPEC-011. **(Mergeado em `main` — PR #15.)**
- **SPEC-013 — Camada de dados 0.2 · Fatia 1: snapshot consultável do mundo** (primeiro banco do projeto; **Fatia 1 de 5** da Fase 0.2): nasce o tier **`services/*`** (borda impura) com o workspace **`@camisa-9/world-store`** que materializa em **Postgres** o `WorldState` que a SPEC-002/009/012 provava **só em memória** — `packages/world-engine` fica **100% puro** (OP-17), dependência unidirecional `store → engine`. **Drizzle** (schema-em-TS, tipos inferidos → zero `any`) + **migration versionada `0000`** (OP-01): `world` (a **`seed` é a fonte-da-verdade**, coluna de 1ª classe) → `world_tier` → `league` → `club` (archetype+weights jsonb) → `athlete`; **ordem canônica** por coluna `ord`; `strength` **derivada** (`clubStrength` na leitura), não persistida. **Mapeadores puros** `WorldState`↔linhas (decompostos, OP-15/16); **`writeWorld`** materializa o snapshot **numa ÚNICA transação** (all-or-nothing); **`readWorld`**/`readClubRoster` reconstroem. **Teste-âncora de reconciliação** (gate de Data): `seedWorld('decada') → writeWorld → readWorld` **deep-equal** ao in-memory **E** `worldHash === world.golden.json.hashes[0]` (`b9d56bdb…`) + **teste de atomicidade** (PK duplicada → rollback total, sem mundo parcial). **Postgres real em CI** (service container `postgres:16`, sem secret/rede externa) + migrate + `docker-compose.yml` local (porta override `POSTGRES_PORT`). Driver **pooled/TCP** (`pg` `Pool`) — nunca HTTP one-shot (travaria a Fatia 2). **91/91 testes** (89 do engine intactos + 2 novos ao vivo; **89/2-skip** sem `DATABASE_URL`), typecheck/eslint/build verdes; **nenhum golden regenerado** (`git diff` dos 4 = 0). **Desvio de mecanismo** (não de escopo): world-store é **typecheck-only** via `tsconfig.typecheck.json` (padrão do `harness/`), **fora** do `tsc -b` — porque `tsc -b --noEmit` proíbe reference cruzada (TS6310); mesmo objetivo, sem quebrar a semântica "typecheck não emite". **Escopo deferido:** atomicidade de rodada/lock/idempotência = **Fatia 2**; viragens = 3; Neon = 4; Pirâmide Elástica = 5. **(Mergeado em `main` — PR #16.)**
- **SPEC-014 — Camada de dados 0.2 · Fatia 2: publicador de rodada transacional** (a joia do money path; **Fatia 2 de 5**): porta o contrato de publicação da SPEC-002 (`RoundStore`/`RoundPublisher`, provado **só em memória**) para **Postgres real** em `services/world-store`, fechando o débito honesto que a memória registrava (*atomicidade de banco / durabilidade pós-crash / lock ficam em aberto até a 0.2*). **Tabela `published_round`** (migration aditiva `0001`, OP-01): **PK composta `(league_id, season_id, round)`** = idempotência durável; `result` **jsonb** (o `RoundResult` inteiro, round-trip byte-exato); `published_at` (audit). **`publishRound`** = **transação interativa** (`db.transaction`): `pg_try_advisory_xact_lock` (→ `locked`; xact-scoped, auto-liberado, sem leak) → checa existência (→ `idempotent`) → `INSERT` → **`await onBeforeCommit?.()`** → commit (→ `published`); falha **síncrona OU assíncrona** no seam → **ROLLBACK real** + rethrow. `readRound` reconstrói. Reusa o contrato **público** do engine (`PublishInput`/`PublishOutcome`/`PublishStatus`) — engine **intocado** (OP-17). **10 testes novos** ao vivo (os 7 comportamentos do `publish.test` in-memory + idempotência durável no-clobber + reconciliação `simulateSeason→publish→read` + invariante de concorrência; teste `locked` **determinístico** segurando o lock numa 2ª conexão — sem flaky). **101/101 testes** (91 intactos; **89/12-skip** sem `DATABASE_URL`), typecheck/eslint/build verdes; migration `0000` e os 4 goldens **intocados** (`git diff` = 0); CI **sem mudança** (o `postgres:16`+migrate da Fatia 1 aplica `0000`+`0001`). **Desvio de mecanismo** (não de escopo): os tipos do contrato já eram públicos no engine → **reusados** (uma fonte de verdade), sem criar `round-types.ts`. **(Mergeado em `main` — PR #17.)**
- **SPEC-015 — Rodadas diárias (1.2) · primeira fatia: âncora diária + orquestrador de tick + rodada-do-mundo atômica** (o primeiro batimento cardíaco do mundo): compõe engine puro + store transacional num loop vivo. **Âncora diária (engine puro):** `anchor.ts` — `MATCH_DAYS` removido, `isMatchWindow = hour===15` (**15h TODO DIA**, 7/7) — alinha o código ao **R4 FINAL ratificado** (inverte a ter/qui/sáb, que era o rótulo do card; mudança **pura**, não toca o PRNG). `anchor.golden.json` regen **aditiva** (9 vetores byte-idênticos + 4 novos dom/seg/qua/sex) por `harness/regen-anchor-golden.ts` com **oráculo independente** que aborta em divergência. **Âncora de temporada (dados):** tabela `season(world_seed, season_id, start_day_index)` (migration aditiva `0002`, OP-01) — o `dayIndex` do round 1 que o snapshot não tem; **input de ops**, destrava o **Model B** (`targetRound = dayIndex − start_day_index + 1`). **Rodada-do-mundo atômica** (decisão do founder #1 — **grão-MUNDO**): `publishWorldRound` = **uma transação** (advisory lock world-day `world:${seasonId}:${round}` + INSERT multi-linha da rodada N de **todas** as ligas + idempotência por `(season_id, round)`) — charter à letra (*a linha do tempo do mundo é all-or-nothing*). **Orquestrador** `runDailyRound(db, seed, epochMs)` → `DailyRoundReport`: guarda de janela → `readWorld` → `readSeasonAnchor` → boundary (`before_season`/`season_complete` **sem viragem** — seam limpo p/ Fatia 3) → publica; **protocolo de falha** (decisão do founder #2 — **derivar**): publish que estoura → tx reverte → `deferred` + log **genérico** (OP-11), "deferido" = ausência da linha (zero estado novo). Engine **intocado** salvo a âncora (`simulateSeason`/`resolveMatch`/`advanceWorld`/PRNG e os goldens `season`/`prng`/`world` = diff 0). **115/115 testes** (89 do engine + 14 novos ao vivo, incl. determinismo de 38 dias = 4×38 rodadas byte-idênticas ao engine, all-or-nothing, idempotência, locked, concorrência; **89/26-skip** sem `DATABASE_URL`), typecheck/eslint/build/prettier verdes. **Desvios de mecanismo de teste** (não de produto): `vitest` `fileParallelism:false` (suítes de integração dividem 1 Postgres e truncam tabelas comuns) + `round-trip.test` apaga `season` antes de `world` (FK nova). **Escopo deferido:** viragem = Fatia 3; encaixe da Copa + scheduler de produção = fatias/deploy de 1.2. **(Mergeado em `main` — PR #18.)**
- **SPEC-016 — Conta + criação do atleta (Fase 1 · card 22)** (a **primeira identidade humana** do projeto — antes o repo era 100% mundo NPC): nasce a lib pura **`packages/player`** (regra determinística, sob o guardrail) + o serviço impuro isolado **`services/player-store`** (persistência + hashing). **Lib:** `allocateAttributes` (a primitiva reusável — 4 focos **Físico/Técnico/Tático/Mental** inteiros em `[20,50]`, **soma fixa 136**), `validateName` (charset PT-BR + blocklist mínima insensível a caixa/acento/**leet**), `validateAppearance` (índices bounded), `validatePassword` (política ≥10), `createAthlete` → `AthleteDraft` (sem id/timestamps). **Standalone** — não importa o `world-engine` em `src` (redeclara `Position`); a calibração cruza com o engine **só no teste**. **Calibração de justiça deliberada:** pool fixo ⇒ **todo atleta nasce overall 34** (fundo da banda tier-4 `34..66` = `WORLD.abilityByTier[3]`), diferindo só no formato. **Serviço:** schema Drizzle no schema Postgres dedicado **`player`** (`account` = e-mail único + `password_hash` argon2id + `athlete` = 4 focos `int CHECK 0..99` + `appearance` jsonb + `training_xp` [o seam da barra de treino do card 13] + `active` + FK→account) + **índice único parcial `(account_id) WHERE active`** (1 atleta ativo/conta); migration `0000_init_player` (OP-01) com tracking em schema próprio **`drizzle_player`** (não colide com o `public.athlete`/`drizzle` do world-store no mesmo DB); `createAccountWithAthlete` = **transação all-or-nothing** (e-mail duplicado → erro **genérico** OP-11 via walk da cadeia de causas do pg); argon2id via `@node-rs/argon2` (baseline OWASP). **28 testes novos** (22 puros + 6 ao vivo) → **143/143** (115 preservados; **22 sempre + 6 gated por `DATABASE_URL`**), typecheck/eslint/build/prettier verdes; `world-engine`/`world-store` **intocados**, nenhum golden regenerado (`git diff` = 0). CI: +passo de migrate do `player-store` (mesmo `postgres:16`). **Escopo deferido:** superfície HTTP/login/sessão = fatia futura; **colocar o atleta no mundo = card 21** (bloqueado pelo snapshot imutável); **treino/evolução = card 13** (design record `docs/projeto/design-atributos-e-evolucao.md` pronto). **Desvios de mecanismo** (não de comportamento): lib standalone (evita TS6059 de reference cruzada) + dois schemas Postgres (`player` tabelas / `drizzle_player` tracking). **(Mergeado em `main` — PR #19.)**
- **SPEC-017 — Atributos e evolução (Fase 1 · card 13)** (o **loop de progressão** — o atleta da SPEC-016 nascia e não crescia): o treino diário enche uma **barra de XP** que, cheia, dá **+1 ponto livre** nos 4 focos (até 99), com **curva de 3 zonas** (várzea rápida → meio → cauda elite). **Model A** (decisão do founder): barra ÚNICA + ponto FLUTUANTE; FOCO do dia = taxa (seam neutro); limiar cresce com o overall. **Lib pura** `packages/player/training.ts` (`trainSession` com cascata + limiar recomputado; `applyPoint` +1 teto 99 — distinta de `allocateAttributes`, que é trava de criação; `nextThreshold` **inteira/piecewise** — guardrail proíbe `exp`/`pow`; `pointsEarnedTotal` = `soma−136+freePoints`, **anti-hoarding**) + bloco `TRAINING` tunável. **Serviço** `services/player-store`: migration aditiva **`0001`** (OP-01, `free_points` int + CHECK ≥0) + `training-repo` (`applyTraining`/`spendFreePoint` em transação com **`SELECT … FOR UPDATE`** — hardening da revisão; `readAthleteProgress`). **Seams neutros** `speedMultiplierPct`/`ageFactorPct` (=100) — DLC e idade adiados. **Revisão adversarial** (workflow 4 dimensões + verificação): 8 achados → 4 confirmados minor/nit (2 "major" de concorrência **rebaixados** — nenhum ponto é fabricável, escritas são overwrite de coluna cheia); fixes = row lock + 4 testes. **168/168 testes** (143 preservados + 21 puros + 4 ao vivo), typecheck/eslint/build/prettier verdes; `world-engine`/`world-store` **intocados**, nenhum golden regenerado; CI sem mudança (migrate do `player-store` já aplica `0001`). **⚠️ Tensão de calibração sinalizada ao founder** (não-bug): os ritmos 3/8/15 e "carreira→~85" do design record são incompatíveis; a calibração atual leva a carreira a ~overall 72 (85+ = grind de lenda), decisão de tunável pendente. **Escopo deferido:** gatilho diário/scheduler/UI/HTTP; efeito diferenciado do FOCO; DLC/idade reais; colocar no mundo = card 21. **(Mergeado em `main` — PR #20.)**
- **SPEC-018 — Cadastro solo/team + código de time (Fase 1 · card R14)** (o **social mínimo do beta** — o projeto tinha identidade individual mas nenhum conceito de GRUPO): nasce o **"monte seu quinteto"**. Um humano se cadastra **solo** (fluxo SPEC-016 intacto, `team_id=NULL`) **ou** cria um **time** (nome + camisa própria → **código distribuível**) **ou** entra com um código, caindo no elenco pela **posição** escolhida. Elenco humano de **até 16** (2 GK · 5 DEF · 5 MID · 4 FWD — espelha `WORLD.squadShape`), marcos no **11º** (primeiro onze) e **16º** (elenco completo → o código **tranca**). **Lib pura** `packages/player/team.ts` (`TEAM` tunável; `validateTeamName` reusa o núcleo do `name-filter`; `validateKit`/`validateCodeFormat`; `isPosition` — guarda o override de posição da borda; `slotsRemaining`/`canClaim`/`humanCount`/`milestone`; `createTeam`→`TeamDraft`; **standalone**, cruza o engine só no teste). **Serviço** `services/player-store`: migration aditiva **`0002`** (OP-01 — tabela `team` + `athlete.team_id` NULL FK) + `team-repo` (`createAccountWithTeam`/`joinTeamWithCode` com **`SELECT … FOR UPDATE`** no time — serializa a corrida pela vaga/16º slot; `lockTeam` só-capitão; `readTeam`; `generateCode` impuro pré-checa unicidade + `UNIQUE` como rede); `player-repo` refatorado p/ **exportar as peças reusáveis** (create conta+atleta SPEC-016), `createAccountWithAthlete` (solo) intocado. **Revisão adversarial** (3 dimensões — concorrência/atomicidade, OP/escopo, cobertura): a concorrência foi **confirmada correta** (`FOR UPDATE`, auto-lock, rollback, auth); achados acionados → **guarda `isPosition`** (autoridade server-side, forward-safe p/ a borda HTTP) + **teste de atomicidade real** (o antigo falhava antes do time ser inserido — vacuidade pega pela revisão) + 5 testes de cobertura. **197/197 testes** (168 preservados + 14 puros + 15 ao vivo; 143 sem `DATABASE_URL`), typecheck/eslint/build/prettier verdes; `world-engine`/`world-store` **intocados**, nenhum golden regenerado; CI sem mudança (migrate do `player-store` já aplica `0002`). **Escopo deferido:** colocar no MUNDO (fundar clube / vaga NPC) = card 21 (+ Fatia 3 da 0.2); rota HTTP/login/Steam auth; química/waiting-list/furar-a-fila = fatias futuras. **(Mergeado em `main` — PR #21.)**
- **SPEC-019 — Pontos de treino com banking (Fase 1 · card 2.7)** (completa o loop de treino da SPEC-017 — o FOCO do dia era **seam neutro**): agora o FOCO importa pelo **ritmo**. Cada sessão treina um foco (**escolhido** ou, sem escolha, o **mais baixo** pelo técnico — `coachFocus`); **repetir** o mesmo foco em dias consecutivos aplica **rendimento decrescente por degraus com piso** (`repeatPenaltyPct` = `max(floor, 100−repeats×step)`, inteira/guardrail); **rotacionar** rende **100%** (o baseline da SPEC-017 — **curva de lenda intacta**, a penalidade só desacelera). O ponto segue **flutuante** (Model A intacto — build é o gasto, treino é o ritmo). **Banking** já existia. **Lib pura** `packages/player/training.ts` (`repeatPenaltyPct`/`coachFocus`/`resolveFocusStreak`; o fator entra como **3º seam** `TrainOpts.focusRepeatPct` default 100 → `TrainState`/`TrainResult` **inalterados** → os 168 preservados por construção). **Serviço** `services/player-store`: migration aditiva **`0003`** (`last_focus`/`focus_streak` + CHECK) + `applyTraining(focus: Focus | null)` (coach default; streak na transação com **`FOR UPDATE`**; a penalidade é **autoridade do servidor**). **Calibração da SPEC-017 resolvida** (decisão do founder): **manter a curva de lenda** (~72 numa carreira; 85+ = lenda multi-carreira) — só a frase do design record corrigida. **Revisão adversarial** (workflow 3 dimensões + verificação): dimensão de **CORREÇÃO = 0 achados**; 6 confirmados **todos minor/nit** (cobertura/redação, **nenhum bug**) → +4 testes (override server-side, concorrência `FOR UPDATE`, piso, saneamento negativo). **207/207 testes** (197 preservados + 4 puros + 6 ao vivo; 147 sem `DATABASE_URL`), typecheck/eslint/build/prettier verdes; `world-engine`/`world-store` **intocados**, nenhum golden regenerado. **Escopo deferido:** FOCO por-foco diferenciado; Forma/Moral/stamina (2.3); gatilho diário/scheduler/UI/HTTP; DLC/idade reais; colocar no mundo = card 21. **(Mergeado em `main` — PR #22.)**
- **SPEC-020 — Entrada por substituição de NPC · fatia gênese (Fase 1 · card 21)** (o **keystone da Fase 1** — a tese "cada humano substitui um NPC" virando código): a **primeira costura `player-store` ↔ `world-store`** e o **primeiro caminho de MUTAÇÃO** do snapshot (antes era só INSERT em massa). Um humano ocupa a vaga do NPC **mais fraco** de uma posição num clube da **divisão de entrada** (tier-4) e passa a existir no mundo — aparece no elenco ao ler o mundo (com nome + `ability`), entra no `clubStrength`, joga a temporada. **Lib pura** `packages/player/ability.ts`: `overall` (extraído do store, fonte única) + `abilityFromFocos` (o mundo vê o seu **overall**; pesos por posição **neutros** = seam wired, guardrail-safe). **`services/world-store`**: migration aditiva **`0003`** (`athlete.is_human` cache + tabela-overlay autoritativa **`world_occupation`** com `human_name`/`ability` **CONGELADOS** → seed+ocupações replayável, honesto) + `occupation-repo` (`occupyNpcSlot` transacional: advisory lock **compartilhado** `world:${seasonId}:1` [rendezvous com `publishWorldRound`, fecha o TOCTOU da gênese] + guarda da gênese + guarda de **tier-4** server-side + vaga do mais fraco via `FOR UPDATE`/empate→`ord` + erros genéricos OP-11). **`services/world-entry`** (workspace novo): `enterWorld` costura (lê player → projeta → ocupa; **sem transação cross-schema** — o `ability` é foto congelada; `isPosition` guarda o `position` cru). **`services/player-store`**: `readAthleteIdentity`. **Evolução de invariante aprovada** ("snapshot é cache" → "seed = NPCs, ocupações = humanos, juntos = replayável"). **Revisão adversarial** (3 dimensões · 3 agentes · verificação): `FOR UPDATE` **confirmado correto** (self-filter do EPQ via `is_human`), determinismo/atomicidade **sólidos**; **2 MAJOR corrigidos** — 6.1 (overlay não carregava os valores congelados → replay desonesto) e **TOCTOU** da gênese (cross-verificado pelos 2 revisores; lock compartilhado) — + guarda de tier-4 + **10 testes** novos. **235/235 testes** (207 preservados + 28 novos: 5 puros de `ability`, 16 de `occupation-repo`, 7 da costura; 151 sem `DATABASE_URL`), typecheck/eslint/build/prettier verdes; **`world-engine` e os 4 goldens intocados** (`git diff` = 0). **Desvio de mecanismo:** costura por **dois handles** (evita mismatch de generics do Drizzle sem `any`). **Escopo deferido:** imunidade na viragem (Fatia 3 — flag `is_human` já plantado); mid-season (congelamento de plano); ponderação por posição; waiting-list/escassez/fundar clube; rota HTTP/auth; idade do humano. **(Mergeado em `main` — PR #23.)**
- **SPEC-021 — Viragem persistida (rollover de temporada) + imunidade do humano (Fatia 3 da 0.2)** (o mundo passa a VIRAR sozinho — fecha o seam `season_complete` da SPEC-015 e co-entrega a imunidade que a SPEC-020 plantou): no fim da temporada o `runDailyRound` dispara a viragem PERSISTIDA numa transação atômica de nível-mundo (promoção/rebaixamento → envelhecer → aposentar ≥35 → transferências → base nova → `seasonId++`), **sobrescreve o snapshot** (overwrite in-place: DELETE ordem-das-FKs → UPDATE `season_id` → INSERT do mundo virado), **re-aplica as ocupações humanas** (o write zera `is_human`), grava o **`turnover_report`** (a auditoria que o overwrite senão apagaria) e **semeia a próxima âncora** (status novo `season_rolled`; o dia da virada é de descanso). **Engine puro:** `advanceWorld(…, immuneIds?)` ganhou um 4º arg opcional (`ReadonlySet<athleteId>`) — `ageAndRetire`/`runTransfers` **pulam** os imunes (guarda no-op **stream-preserving**); o engine fala de IDS, não de "humano". **`world.golden.json` BYTE-IDÊNTICO** (set vazio ⇒ stream/serialização inalterados — critério de aceite DURO; engine-aware SEM regen de golden). O humano **sobrevive** (não aposenta/transfere) e **sobe/desce com o clube** (o `club_id` é estável na promoção). **`services/world-store`:** `readWorldOccupations` + migration aditiva `0004` (`turnover_report`) + `persistWorldTurnover(db, seed, results, dayIndex)` (advisory lock de rollover + idempotência por `season_id`; falha → ROLLBACK total → `deferred`). **Revisão adversarial** (workflow · 4 dimensões · 19 agentes · verificação): **1 MAJOR real cross-confirmado e CORRIGIDO** — o rollover atrasado (deferido→retry) pulava a rodada 1 da nova temporada porque a âncora vinha do calendário ideal; fix = **`newStart = dayIndex + 1`** (o dia SEGUINTE ao dia REAL da virada, atrasado ou não); +3 testes (determinismo `immuneIds` não-vazio, rollover atrasado, concorrência double-roll); 1 race estreito documentado (temporada 100% deferida); nits (idade sem teto → Regen; swap suprimido = design). **247/247 testes** (235 preservados + 12 novos), typecheck/eslint/build/prettier verdes; **`world.golden.json` e os demais goldens intocados** (`git diff` = 0). **Escopo deferido:** o **Regen** (renascimento/Hall of Fame — card a criar), o re-baker do `ability` na virada, o encaixe da Copa, o scheduler de produção. **(Mergeado em `main` — PR #24.)**
- **SPEC-022 — Regen: renascimento de carreira + Hall of Fame (Fase 1 · roadmap 4.2 — 1ª fatia)** (fecha o arco "da várzea às lendas" — a carreira humana passa a ter FIM, e o fim vira começo): quando a carreira encerra (**forçado aos 42**, ou **por escolha ≥25** via `requestRegen`), a carreira antiga vira **lenda permanente** no Hall of Fame (nome antigo preservado) e nasce um **atleta novo ativo** na mesma conta — jovem (17), atributos resetados (overall 34), com um **banco de pontos de LEGADO** = `floor(pontos da carreira anterior × 25%)` — o gancho de FOMO/compra, com o **paywall isolado num seam** (`canRegen`, default permitido). **Entrada aos 17 corrigida** (o `occupyNpcSlot` grava `age: WORLD.youthAge` — a idade herdada do NPC na SPEC-020 era decisão provisória, não do founder → a idade vira o relógio de carreira). **Lib pura** `packages/player/regen.ts` (`regenLegacyPoints` inteira/guardrail-safe + `REGEN.legacyPct`). **`services/world-store`:** migration aditiva **`0005`** (tabela **`legend`** PK `(world_seed, human_athlete_id, season_ended)` — N lendas/humano + `world_occupation.regen_requested`) + `legend-repo` (`archiveLegend`/`readLegends`/`readRegenEligible`) + `requestRegen` (trava ≥25) + **`reassignSlot`** (reatribuição atômica da vaga). **`services/player-store`:** `rebirthAthlete` (velho→inativo, nasce o novo ativo + banco de legado; idempotente). **`services/regen`** (workspace novo): `runRegenPass`/`regenAthlete` (a costura pós-virada, isolamento por candidato). **Revisão adversarial** (workflow 3 dimensões + verificação): a dimensão gatilho/legado voltou **limpa**; os achados estavam **todos na orquestração** → **1 CRITICAL corrigido** (a ordem `vacate→reoccupy` da SPEC abria uma **janela órfã irrecuperável** — um humano pagante caindo do mundo em silêncio → resolvida por construção com o `reassignSlot` in-place, a ocupação nunca é deletada) + **3 MAJOR** (isolamento por candidato; o flag do regen voluntário não sobrevivia à viragem → `reapplyOccupations` re-aplica `regen_requested`; idempotência same-slot) + endurecimento (OP-11 no rebirth; guarda de gênese no reassign). **Desvio de mecanismo** (não de produto): o critério #5 ("vaga reverte a NPC") virou reassign atômico in-place — mesmo estado final observável (renascido no mesmo clube, velho fora do mundo), sem a janela órfã (elimina de brinde o risco "Loop de vaga" da própria SPEC). **263/263 testes** (247 preservados + 16 novos, incl. voluntário ponta-a-ponta, recuperação pós-crash, guarda de gênese; sem `DATABASE_URL`: puros sempre, ao vivo skip), typecheck/eslint/build/prettier verdes; **`world-engine` e os 4 goldens intocados** (`git diff` = 0; único toque = exportar `athleteName` do barrel). **Escopo deferido:** paywall real; UI de rename; wiring do scheduler; regen mid-season; durabilidade transacional distribuída cross-schema (best-effort idempotente, recuperação por passe). **(Mergeado em `main` — PR #25.)**
- **SPEC-023 — Congelamento de vaga (30 dias)** (a **mecânica de retenção/escassez** — o pilar "escassez via waiting list" ganha dentes): quando um humano fica inativo, a vaga dele **congela** (dispara o e-mail "estamos segurando sua camisa" — seam), e se ele não voltar em **30 dias**, a vaga **reverte a NPC** (volta à fila); quem volta a tempo **descongela** e segue. O congelado **ainda joga** com a ability congelada (o mundo joga com ou sem você → **snapshot imutável, engine INTOCADO**); reverter = **"benched"** (a vaga vira NPC via `vacateSlot`, mas o atleta no player-store é **preservado** e re-ocupável — desacoplado do Regen). Fatia **SÓ-MUNDO** (zero cross-schema, sem workspace novo): migration aditiva **`0006`** (`world_occupation.last_active_day` + `frozen_since_day`, o relógio em day-index) + **`vacancy-repo`** (`markActive` = o seam de atividade que grava/descongela; `runVacancyPass` = o passe diário que congela/reverte, molde do `runRegenPass`; `readVacancyState`) + `VACANCY = { revertAfterDays: 30 }`. Os 2 campos novos entram no `reapplyOccupations` → **sobrevivem à viragem** (2º caso do gotcha da SPEC-021). **Revisão adversarial** (workflow 3 dimensões + verificação): a dimensão viragem/OP/escopo voltou **limpa**; os achados estavam **todos na concorrência** → **1 CRITICAL corrigido** (TOCTOU no revert: o passe decidia no snapshot e o `vacateSlot` deletava sem re-checar → um `markActive` concorrente no dia 30 expulsava um humano ativo/pagante do mundo → resolvido com **`revertIfStale`, um DELETE condicional atômico** que re-checa `last_active_day` no WHERE) + **2 MAJOR** (TOCTOU no freeze [+ `AND last_active_day < currentDay`]; `onFreeze` que lança perdia o e-mail → hooks disparam **dentro da tx**, lança faz ROLLBACK → retenta) + cobertura (+6 testes: onFreeze rollback, boundary 29/30, multi-ocupante, isolamento, anti-TOCTOU, no-op). **Desvio de mecanismo** (nit): o ramo de thaw no passe (escopo C) era inalcançável (o `markActive` é o único a limpar o frozen) → removido (código morto), o thaw é 100% do `markActive`. **276/276 testes** (263 preservados + 13 novos), typecheck/eslint/build/prettier verdes; **`world-engine` e os 4 goldens intocados** (`git diff` = 0; zero toque no engine/player-store). **Escopo deferido:** e-mail real (outbox); sinal de atividade real (HTTP/sessão); wiring do scheduler; a waiting-list que puxa a vaga liberada; restaurar a identidade de NPC na vaga revertida (regenera na viragem). **(Mergeado em `main` — PR #26.)**
- **SPEC-024 — Salário e estilo de vida (Fase 1 · roadmap 2.8)** (o **stake material** da carreira — casa com a retenção da SPEC-023): o desempenho vira **poder de compra**. O atleta ganha **salário por rodada** (`f(overall)`) + **prêmios** (vitória>empate>derrota) → um **saldo**; gasta num **catálogo ABERTO** (`PURCHASES` tunável) de compras com **trade-off narrativo DECLARADO** (carro=+moral/+fama/+risco; academia=+físico/−vestiário — aplicado pela **2.3/F2** via `aggregateTradeoffs`, aqui só DADO); sobe a **escada de moradia** (pensão→quitinete→casa→cobertura = o patrimônio da faixa) e alcança o marco da **casa da mãe** (card = seam). **Duas travas NUNCA** (invariantes, provadas por teste): **anti-dinheiro-real** (o saldo só cresce por `accrueRound`; zero seam de moeda real) + **nunca loja de stats** (nenhuma compra escreve nos focos — provado na compra E no crédito). Fatia **SÓ-player-store** (zero cross-schema — o prêmio é param): **lib pura** `packages/player/economy.ts` (fórmulas inteiras + catálogo + `validatePurchase`/`lifestyleTier`/`aggregateTradeoffs`) + **`services/player-store`** (migration aditiva **`0004`** — `athlete.balance` CHECK≥0 + tabela `purchase` PK `(athlete_id,item_id)`; `economy-repo` = `accrueRound`/`purchaseItem` [compra **atômica** com `FOR UPDATE`]/`readWallet`). **Revisão adversarial** (workflow 3 dimensões + verificação): as verificações-de-segurança voltaram **positivas** (compra atômica sem saldo negativo no double-spend; double-buy barrado; moradia sem pulo de degrau); achados → **fix OP-11** (erros de constraint do pg [CHECK/PK] agora genéricos, causa só p/ log) + **cobertura** (+teste de concorrência, soma-de-chave-repetida, boundary de saldo exato, `accrueRound` não toca focos). **⚠️ Débito honesto sinalizado ao founder:** `accrueRound` sem chave de idempotência de rodada **contradiz o charter** (job idempotente/retry-safe) — sob um scheduler real, retry = **pagamento em dobro no money path**; o §104 da SPEC ("gatilho diário garante 1×") estava errado → a idempotência (ledger `(athlete_id, season_id, round)`, molde do `publishWorldRound`) é **obrigatória no card do scheduler**, antes de costurar. **Gotcha (recorrência SPEC-015):** a nova tabela-filha `purchase` (FK→athlete) exigiu `delete(purchase)` antes de `delete(athlete)` no `wipeAll` de TODA suíte que apaga o pai (player-repo/training-repo/team-repo/regen/world-entry) — não só a nova. **299/299 testes** (276 preservados + 23 novos), typecheck/eslint/build/prettier verdes; **`world-engine`, os 4 goldens E o world-store intocados** (`git diff` = 0). **Escopo deferido:** aplicar os efeitos (2.3/F2); o ledger de idempotência (card do scheduler); o gatilho real + a costura resultado→prêmio; a UI da faixa + o card; contratos/luvas. **(Mergeado em `main` — PR #27.)**
- **SPEC-025 — Decisões de carreira (o motor · Fase 1 · roadmap 2.4)** (a **agência das 18h** do Dia do Jogador): 3-5 decisões/dia **gatilhadas pelo estado** do atleta (overall/saldo/patrimônio; idade = seam do mundo; moral = seam da 2.3, inerte), de um **catálogo ABERTO** (`DECISIONS` tunável), respondidas pelo jogador (`answerDecision`) **ou** resolvidas conservadoramente pelo agente no deadline das 18h (`resolveDeadline`), cada uma virando **história no log do perfil**. **Efeitos = seam declarado** (o `outcome` fica no log; a 2.3 aplica moral); a **proposta de transferência é REGISTRADA** (o card 1.4 executa o mercado/janela — **fatiamos com o founder** para não duplicar o roadmap 1.4). Fatia **SÓ-player-store** (zero cross-schema): **lib pura** `packages/player/decisions.ts` (geração determinística por hash **FNV-1a de 32 bits via shifts** — guardrail-safe; `generateDailyDecisions`/`conservativeOption`) + **`services/player-store`** (migration aditiva **`0005`** — tabela `decision` com `ord`/status/outcome; `decision-repo` = `generateForDay` [idempotente, **lock advisory** athlete-dia]/`answerDecision` [`FOR UPDATE`]/`resolveDeadline`/`readDecisionLog`). **Revisão adversarial** (workflow 3 dimensões + verificação): **nenhum CRITICAL/MAJOR de código** (núcleo sólido); corrigidos → **TOCTOU** na geração (check-then-insert → **lock advisory**, o padrão do repo), **ordem** intra-dia não-determinística (→ coluna **`ord`** = rank de geração persistido), **fallback órfão** (`conservativeOption` cai na 1ª opção + invariante testada), **seam de idade** sem consumidor (→ template `veterano`) + **5 MAJOR de cobertura** (variabilidade, idempotência com contexto-mudado, boundary N=3-5, concorrência responder×resolver, auth cross-atleta OP-09, seam de idade). **319/319 testes** (311 preservados + ~14 novos), typecheck/eslint/build/prettier verdes; **`world-engine`, os 4 goldens E o world-store intocados** (`git diff` = 0). **Escopo deferido:** aplicar os efeitos (2.3); executar a transferência (card 1.4); toast/UI; wiring do scheduler; gatilhos de moral/forma (2.3). **(Mergeado — PR #28.)**
- **SPEC-026 — Lesões narrativas (o arco · Fase 1 · roadmap 2.5)** (a lesão vira **arco narrativo**, nunca punição cega): a contusão acontece (raramente — via um evento de partida futuro), o atleta **recupera** ao longo de dias e **volta por cima**, e no meio ela **gera uma decisão** ("forçar a volta ou respeitar o prazo?") via o motor da SPEC-025 e vira **história no perfil**. **Fatiamos com o founder para blindar o money path:** a **ocorrência** é seam (`injureFromMatch` — a **partida rica** [card 1.1/3.2] injeta; nos testes é chamada direto) → **`world-engine` e os 4 goldens INTOCADOS** (`git diff` = 0, o critério duro das 7 últimas SPECs). Fatia **SÓ-player-store** (zero cross-schema — `available` é seam): **lib pura** `packages/player/injury.ts` (o arco determinístico: `recoveryDaysFor`/`injuryPhase`/`isAvailable`/`comebackOutcome` — o comeback é **constante-seam** que a 2.3 aplica no evento `recovered`) + **`decisions.ts`** (extensão: `injured?` no `DecisionContext` + template `lesao-volta`, molde do `age` — os 168+ preservados) + **`services/player-store`** (migration aditiva **`0006`** — tabela `injury` + índice único parcial `(athlete_id) WHERE status='active'`; `injury-repo` = `injureFromMatch` [o seam da ocorrência]/`advanceRecovery` [o passe diário que fecha o arco]/`readInjuryState` [lesão ativa + `available`]/`readInjuryLog` [a história]; `decision-repo` estende `generateForDay(extra: {age?, injured?})` **sem** ler o injury-repo — o scheduler wira). **Revisão adversarial** (workflow 3 dimensões + verificação de cada achado): **1 MAJOR real cross-confirmado e CORRIGIDO** — a inconsistência dos dois seams (`readInjuryState.available` derivava por **DIA**, mas o guard "1 ativa" de `injureFromMatch` era por **STATUS**: uma lesão vencida-mas-não-fechada pelo passe lia `available=true` **mas barrava** uma nova → a lesão nova sumia em silêncio e o atleta jogava numa lesão stale) → resolvido com **transação + lock advisory (atleta) + fecha-lazily** a vencida antes de checar/inserir (reconcilia `status`↔`dia`; de brinde serializa a concorrência e **elimina** o `isUniqueViolation` frágil); nits endurecidos (comentário do comeback; fail-open documentado) + **5 lacunas de cobertura** (boundary 109, re-lesão pós-recovery, idempotência de `advanceRecovery`, isolamento cross-atleta, read-em-prazo-vencido). **338/338 testes** (331 preservados + 7 novos), typecheck/eslint/build/prettier verdes; **`world-engine` e os 4 goldens intocados**; o `world-store` intocado. **Escopo deferido:** a ocorrência real (partida rica = card 1.1/3.2); tirar o humano da partida (o mundo lê `available`); aplicar a forma/o comeback (2.3); o efeito da escolha na lesão (`forceReturn`); toast/UI/jornal; o wiring do `advanceRecovery` no scheduler. **(Mergeado — PR #29.)**
- **SPEC-027 — Forma & Moral (as duas barras vivas · fatia A · Fase 1 · roadmap 2.3)** (o **card de PAYOFF** — liga os seams que as 3 SPECs anteriores plantaram): as **duas barras persistentes do R4** (Forma e Moral) ganharam vida no player-store, **consumindo** o `aggregateTradeoffs` das compras (SPEC-024), o `outcome.moral` das decisões (SPEC-025) e o `comebackOutcome` da lesão (SPEC-026). **Modelo baseline + offset + evento que decai** (decisão do founder): cada barra é `[0,100]`, baseline 50; os **eventos** entram como bumps **na fonte** (na tx do repo-fonte — `answerDecision`/`resolveDeadline` → moral, `advanceRecovery` **e** o lazy-close do `injureFromMatch` → comeback moral, `applyTraining` → forma), e o **passe diário** (`applyDailyMood`) faz a barra **decair** rumo ao alvo (Moral rumo a `baseline + offset do estilo de vida`, saturado ±`lifestyleClamp`; Forma rumo a `baseline`, rebaixado `injuryFormaDrag` enquanto recuperando). O **payoff concreto:** `buildContext` lê o `moral` real → `DecisionContext.moral` → o `crise-moral` (e as decisões de moral) **deixam de ser inertes**. **"Aplicar já" fatiado em 2** (decisão do founder): **fatia A** (esta) = as barras vivas+consumidas, **só-player-store** (lib pura `packages/player/mood.ts` + `mood-repo` + migration aditiva **`0007`** — `athlete.forma`/`moral`, default 50, CHECK `[0,100]`); **fatia B (SPEC-028, logo em seguida — card a criar)** = a **aplicação na performance da partida** (`effectiveAbility(base, forma, moral)` + modulação in-memory da ability humana no `runDailyRound` + recompute de `clubStrength`; a base congelada da SPEC-020 fica intacta, goldens byte-idênticos). **Revisão adversarial** (workflow 3 dimensões + verificação): **1 minor real cross-confirmado e CORRIGIDO** — a transição `active→recovered` tem **dois produtores** (`advanceRecovery` **e** o fecha-lazily do `injureFromMatch`), mas só o primeiro aplicava o `+comeback` → uma re-lesão antes do passe engolia o bônus em silêncio; corrigido (ambos aplicam, exatamente 1×) + **calibração endurecida** (`trainFormaBump` 4→6, `>` `decayStep`, senão o passe do mesmo dia anulava o treino) + **7 lacunas de cobertura** (concorrência de `answerDecision` [nenhum bump perdido — `FOR UPDATE`], moral da conservadora no `resolveDeadline`, convergência **com offset** [satura, não no baseline], boundary do drag da lesão, decisão sem-moral, piso do clamp, o comeback no lazy-close). **363/363 testes** (338 preservados + 7 puros + 18 ao vivo), typecheck/eslint/build/prettier verdes; **`world-engine` e os 4 goldens intocados** (`git diff` = 0); o `world-store` intocado. **Escopo deferido:** a aplicação na partida (fatia B/SPEC-029); o passe no clock real + a **idempotência estrita por dia** (concern do scheduler, como o `accrueRound`); stamina (partida rica); os 12 atributos; `fama`/`risco` (outros seams). **(Mergeado — PR #31.)**
- **SPEC-029 — Forma & Moral: aplicar na partida (fatia B · Fase 1 · roadmap 2.3)** (fecha o "aplicar já" — as barras da fatia A passam a **afetar o resultado**): Forma e Moral **modulam a ability efetiva do humano na PARTIDA** — um jogador em fase/moral altas puxa o `clubStrength` pra cima, em má fase pra baixo. **Renumerada de 028** (o 028 virou a página Coming Soon da Steam do 24bit, PR #30). **Lib pura** `packages/player/mood.ts`: `moodAbilityPct` (centrado em 100, peso **60/40** forma/moral, swing **±12%**, **simétrico** — trunca a magnitude rumo a zero, sem o viés do `floor` do sinal) + `effectiveAbility(base, forma, moral)` = `floor(base × pct/100)`, clamp [0,100]. **`services/world-store`:** `applyMoodToWorld(world, abilityByAthleteId)` (**puro** — reconstrói o `WorldState` sobrescrevendo a ability + recomputa `clubStrength` dos clubes afetados; no-op vazio; **reusa `clubStrength` já exportado → engine INTOCADO**) + `runDailyRound(db, seed, epochMs, modulate?)` com o 4º param **OPCIONAL** `modulate?: WorldModulator` (default identidade → o world-store fica **DESACOPLADO** do player-store; os 12 testes NPC não mudam). **`services/player-store`:** `readMoodByIds` (batch). **`services/world-entry` (a costura injetável):** `moodModulator(worldDb, playerDb, seed)` lê `readWorldOccupations` + `readMoodByIds` → `effectiveAbility(base CONGELADA, forma, moral)` → `applyMoodToWorld` — é o que o **scheduler** injeta. TUDO in-memory: a **base congelada de `world_occupation.ability` (SPEC-020) e os 4 goldens ficam INTACTOS** (`git diff` = 0; a lógica do engine não é tocada). **Revisão adversarial** (workflow 3 dimensões + verificação): **1 MAJOR real cross-confirmado e CORRIGIDO** — o seam `modulate` do `runDailyRound` (o ponto de integração com o **money path**) nunca era exercitado (todos os testes com 3 args) → uma regressão que passasse o mundo NÃO-modulado deixaria Forma/Moral fora da rodada publicada sem quebrar teste; fix = teste ao vivo que prova que a rodada **PUBLICADA** = a sim modulada e ≠ a pura. + **nit da simetria** corrigido. + **débito sinalizado (minor de replay):** forma/moral são inputs **mutados in-place** todo dia sem histórico por rodada → uma rodada humana publicada **não é recomputável dos inputs**, só do `published_round.result` (durável) → a garantia do charter muda de "recomputar dos inputs" p/ "confiar no resultado gravado"; **snapshot de mood por rodada = card de auditoria futuro** (como a idempotência do `accrueRound`). **377/377 testes** (363 preservados + 14 novos), typecheck/eslint/build/prettier verdes; **a lógica do `world-engine` e os 4 goldens intocados**; sem migration (fatia in-memory). **Escopo deferido:** a química (colegas); o snapshot de mood por rodada (replay/auditoria); o wiring no scheduler; stamina; os 12 atributos. **(Mergeado — PR #32.)**
- **SPEC-030 — Scheduler de produção (o tick diário · roadmap 1.2)** (o **batimento cardíaco de produção** — o mundo passa a jogar às 15h SOZINHO; e o débito de idempotência das SPEC-024/027 é PAGO): nasce o serviço greenfield **`services/scheduler`** com **`runDailyTick(worldDb, playerDb, seed, epochMs)`** — a orquestração que dirige, IDEMPOTENTE, todos os passes do mundo/dos humanos numa passada: `runDailyRound` (+`moodModulator`) → na virada `runRegenPass` → `runVacancyPass` → **loop por ocupação humana** (`readWorldOccupations` = a lista de trabalho; o player-store não enumera seus humanos): **accrue [salário+prêmio, SÓ em dia com rodada publicada]** · mood (decay) · `resolveDeadline(ontem)` · `generateForDay(hoje)` · `advanceRecovery`. **Isolamento por-humano** (`safeHumanPasses` — um erro não aborta o tick). O **`main.ts`** = a BORDA impura (o único `Date.now()`; `services/*` está fora do guardrail); o `epochMs` é injetado no tick (testável, molde `epochAt`). O deploy (cron) só invoca `node main.js` 1×/dia. **O ledger de idempotência (o débito):** `accrueRound(db, athleteId, day, result?)` e `applyDailyMood(db, athleteId, day)` ganham o **claim** `insert(daily_ledger {athlete_id, day, scope}).onConflictDoNothing().returning()` na própria tx (migration aditiva **`0008`**, PK `(athlete_id, day, scope)`) — 0 linhas = já rodou hoje → no-op; o crédito/decay + o claim commitam JUNTOS (crash → rollback des-reivindica). **Rodar o mesmo dia 2× = no-op.** O **prêmio** (`prizeForClub`) lê a rodada publicada → acha o jogo do clube do humano → win/draw/loss → `matchPrize` (`buildClubLeagueMap` roteia por liga). **Revisão adversarial** (workflow 3 dimensões + verificação): **2 MAJOR reais no money path, raiz ÚNICA — o accrue não era gated por `paid`:** (a) **salário pago em dia SEM rodada** (viragem/before-season/deferred/locked — contradizia o §147 da SPEC); (b) **prêmio PERDIDO no deferred-retry** (o dia que DEFERE reivindicava o ledger com salário-só → o retry que publica achava o dia já reivindicado → o bônus sumia). **Fix (um só):** o accrue **só roda quando `paid`** (`status ∈ {published, idempotent}`) → nada de salário sem jogo + o dia deferido não reivindica o ledger (o retry paga salário+prêmio). **+6 testes** (viragem→saldo 0; deferred→retry→prêmio intacto; múltiplos humanos [roteamento]; isolamento; draw/loss; cross-day) + minor (`decisionsGenerated`→`decisions`, honesto). **388/388 testes** (382 preservados + 6 novos), typecheck/eslint/build/prettier verdes; **lógica do `world-engine` + os 4 goldens intocados** (`git diff` = 0); o `world-store` intocado. **Escopo deferido:** a infra de cron/deploy (dispara o `main` — card de ops); multi-seed (config); o sinal de atividade (`markActive` via HTTP); o seam `age` no `generateForDay`; stamina; os 12 atributos. **(Mergeado em `main` — PR #33.)**
- **SPEC-031 — Partida rica: ocorrência de lesão (Fase 1 · roadmap 1.1/3.2 — 1ª fatia)** (a partida finalmente **MACHUCA** — ativa o arco da SPEC-026, e o card "que tocaria o engine/golden" sai **de graça**): o `injureFromMatch` (o seam órfão que a SPEC-026 plantou, que **ninguém chamava**) passa a **disparar** de uma ocorrência de partida determinística → o arco de lesão (contusão → recupera → volta por cima) ACONTECE em produção. **A tese central: enriquecer a partida SEM tocar o money path.** **`packages/world-engine`:** o `MatchResult` ganhou `events?` (opcional) + a lib pura **`match-events.ts`** (`matchInjuries(homeClubId, homeRoster, awayClubId, awayRoster, rng)` — decide raro [~4%/lado], sorteia o atleta [do elenco], a gravidade [ponderada leve>media>grave], o minuto; `MATCH_EVENTS` tunável; inteiro/guardrail-safe; **NÃO altera o placar**). O enriquecimento vive no **`world-season.ts`** (o único lugar com os **elencos** — `resolveMatch` só vê força escalar), com um **RNG SEPARADO** (`createRng(deriveSeed(seed, leagueId, seasonId, round, homeId, awayId, 'events'))` — a **7ª parte** `'events'` faz um stream **disjunto** das chaves de placar de 6 partes) → **`resolveMatch`/`simulateSeason` intocados e os 4 goldens byte-idênticos** (o critério DURO das últimas 8 SPECs; provado pelo teste **SCORE-NEUTRAL** = a temporada enriquecida, tirados os `events`, é placar/tabela byte-idêntica ao `simulateSeason` puro). **`services/scheduler` (a borda injeta):** `roundOutcomes` colhe prêmios E lesões da rodada **publicada**; `injuryFor` acha o evento cujo `athleteId === occ.athleteId`; `runHumanPasses` chama `injureFromMatch(playerDb, occ.humanAthleteId, day, severity)` (mapeando id-do-mundo → id-do-player) **ANTES** dos demais passes → o `injured` do dia já reflete na geração (o **payoff:** humano lesionado → o tick gera `lesao-volta`). NPC lesionado → não persiste (só humanos). **Revisão adversarial** (workflow 3 dimensões + verificação): o **núcleo voltou SÓLIDO** (determinismo/RNG-separado/golden/guardrail confirmados); **1 robustez real, CORRIGIDA** — uma lesão que LANÇA (gravidade corrompida no jsonb, falha transitória) rodava sem isolamento e **starvaria** os demais passes do humano no dia (mood/decisões/recuperação) → **`tryInjure`** isola (try/catch, log OP-11, best-effort) + cobertura (**PAYOFF**/**SCORE-NEUTRAL**/**NPC não-persiste** não eram assertados) + **débito latente da SPEC-030 corrigido** (o `wipeAll` do teste do scheduler não limpava `turnover_report` → a linha da viragem acumulava entre runs → o `season_rolled` virava `deferred`). **402/402 testes** (388 preservados + 14 novos: 5 puros de `match-events`, 4 de `world-season` [incl. score-neutral], 5 ao vivo do tick [seam, idempotência-lesão, NPC, robustez, payoff]), typecheck/eslint/build/prettier verdes; **`resolveMatch`/`simulateSeason` e os 4 goldens intocados** (`git diff` = 0); **sem migration** (jsonb schemaless); o `injury-repo` só é chamado. **Escopo deferido:** o timeline de gols (`kind:'goal'`); os eventos de ESCOLHA interativos (3.2); a lesão AFETAR o placar (reescreveria `resolveMatch`); ponderar o atleta lesionado. **(Mergeado em `main` — PR #34.)**
- **SPEC-032 — Cron/deploy do scheduler + catch-up completo (roadmap 1.2)** (o mundo joga às 15h SOZINHO num relógio real — o `runDailyTick` da SPEC-030 estava pronto mas ninguém o ligava): entrega **(a)** o artefato de deploy e **(b)** o **catch-up completo** que honra o guardrail *uptime de rodada 100%*. **`packages/world-engine`:** `dueDayIndex(epochMs)` (novo export PURO — o maior dia cuja rodada das 15h venceu; `anchor.golden.json` byte-idêntico). **`services/world-store`:** migration **`0007_tick_progress`** (`tick_progress(world_seed PK, last_day_index)` — o cursor resumível = o último dia cuja RODADA DO MUNDO liquidou) + `tick-progress-repo` (`readTickCursor`/`advanceTickCursor` monotônico via `greatest`) + `runRoundForDay(dayIndex)` extraído de `runDailyRound` (o núcleo por-dia; re-lê o mundo/âncora a cada dia → pós-viragem já vê o novo). **`services/scheduler` (o coração):** `runDailyTick` vira catch-up-aware — `to=dueDayIndex`, `from=min((cursor ?? seasonStart−1)+1, to)`, loop `[from..to]` com `processDay` (o dia corrente SEMPRE re-processado idempotente; os perdidos ANTES dele recuperados); o cursor avança só no **settle da rodada** (o mundo nunca trava por um humano quebrado) e um dia **deferido PARA** o loop (retenta). **Deploy:** `Dockerfile` (worker em container, `tsx`) + `.dockerignore` **na raiz** + `docs/ops/scheduler-deploy-runbook.md` (Railway/Render cron `0,30 18-23 * * *` UTC = 15h BRT) + `.env.example`; **o gap de produção fechado** — os 4 `services/*` ganharam `"exports":"./src/index.ts"` → resolvíveis via `tsx` fora do vitest (o `main.ts` smoke-testado ao vivo). **Revisão adversarial** (workflow 3 dimensões + verificação): **2 MAJOR reais corrigidos** — **(1) regen órfão numa viragem falha:** o regen era gateado só em `season_rolled` (status transitório); se a viragem committasse mas o pass falhasse (ex.: `readRegenEligible` cai) e o cursor travasse, o retry via `before_season` NÃO re-rodava o regen → um humano ≥42 jogaria uma temporada a mais → **fix:** gate na **janela de gênese** (`season_rolled || before_season`), que auto-cura (descoberta: o `reassignSlot` tem guarda de gênese que barra o regen em dia PUBLICADO, então "todo dia" estava errado); **(2) `.dockerignore` vazava segredo:** um `.dockerignore` em `services/scheduler/` é **INERTE** p/ contexto=raiz (o Docker lê o da raiz) e não cobria `.env.*` → **fix:** `.dockerignore` na RAIZ cobrindo `**/.env*` (OP-02/OP-12). + **1 MINOR** (buraco silencioso no 1º tick com âncora no passado → init `seasonStart−1`, o backfill que a SPEC já pedia). **415 testes** (388 preservados + 27 novos), typecheck/eslint/build/prettier verdes; **a lógica do `world-engine` e os 4 goldens intocados** (`git diff` = 0; o catch-up é 100% borda). **Escopo deferido:** multi-seed; **executar o deploy de fato** (ação de ops); scheduler interno always-on; imagem mínima; alerta ativo em falha; reconciliação de passe humano transitório. **(Mergeado em `main` — PR #35.)**
- **SPEC-033 — Transferências: o humano como ALVO do mercado (Fase 1 · roadmap 1.4 — 1ª fatia)** (dá vida ao `outcome.transfer` inerte da SPEC-025; o pilar "cooperação, não gestão" virando código): **o humano é o ALVO do mercado, nunca o operador** (o clube é palco NPC). **`packages/player`:** `transfer.ts` (`isTransferTarget(overall, tier)` — forte-para-o-tier; `bandMaxByTier` redeclara `WORLD.abilityByTier`, cruzado no teste) + o template `proposta-clube-maior` (o `tier`/`marketOpen` entram como seams no `DecisionContext`). **`services/player-store`:** migration `0009` (`transfer_requested`+`market_open`); `answerDecision` marca a pendência ao aceitar; `leaveTeam` (o racha do quinteto). **`services/world-store`:** `transferOccupation` — o **MOVE ATÔMICO** (occupy-destino + vacate-origem numa tx, **sem janela órfã**, na janela de gênese, carregando a idade de carreira + a força VIVA) + `pickTransferDestination` (puro, determinístico, **NPC-only**). **`services/transfer` (workspace novo):** `runTransferPass` — na gênese, move os humanos que aceitaram. **`services/scheduler`:** fia o pass na gênese (após o regen) + passa o `tier` ao `generateForDay`. **Revisão adversarial** (workflow 3 dimensões + verificação de cada achado): **5 MAJOR reais (3 raízes), todos corrigidos** — **(1) divergência heurística↔move:** o pick contava HUMANOS (o `WorldState.Athlete` não tem `isHuman`) mas o move só troca NPC → enfraquecia o destino / travava → **fix: NPC-only no pick** (exclui os slots humanos); **(2) move em dobro:** a flag limpa DEPOIS do move (não-idempotente) → crash/erro no meio = 2ª transferência → **fix: clear-first (at-most-once)**; **(3) vivo vs. congelado:** a proposta gatilha no overall VIVO mas o destino usava a ability CONGELADA → sumia pra quem cresceu → **fix: a força VIVA (`abilityFromFocos`) no destino E gravada na nova vaga** (a transferência reconhece o crescimento). + mundo fresco por-candidato; 1 MINOR (`leaveTeam` após o move) + 1 NIT (`market_open` persiste) aceitos/deferidos. **429 testes** (415 preservados + 14 novos), typecheck/eslint/build/prettier verdes; **`world-engine` (`runTransfers`) e os 4 goldens intocados** (`git diff` = 0). **Escopo deferido:** o mercado NPC rico (regenera o golden — Fatia 2); termos de contrato; mid-season; química; reset de `market_open`. **(Mergeado em `main` — PR #36.)**
- **SPEC-034 — Entrada imediata (solo) + waiting-list (Fase 1 · R14/retenção)** (o pilar "escassez via waiting list" ganha dentes; fecha o loop da SPEC-023 — a vaga revertida a NPC agora é HERDADA): **o solo entra IMEDIATO** (mid-season, sem esperar a viragem). **Descoberta central (investigação por workflow):** a guarda de gênese do `occupyNpcSlot` era **conservadora demais** — sua justificativa ("a re-simulação reescreveria rodadas publicadas") é FALSA (o `publishTarget` só publica a rodada-alvo, idempotente; as passadas nunca são reescritas; o occupy só muta o INPUT futuro). Veredito **SAFE**, money-path intocado; o único trade-off (a tabela da viragem re-simula com o entrante) é da **classe SPEC-029** e curável pela auditoria futura. **`services/world-store`:** `OccupyInput.allowMidSeason?` → `occupyNpcSlot` gateia `if (!allowMidSeason) assertGenesis` (**só a entrada solo passa**; regen/transfer ficam gênese-gated) + migration **`0008_waiting_list`** (fila FIFO) + `waiting-repo` (`enqueue`/`dequeue`/`countEntryHumans`/`findEntryClubWithSlot`; `WAITINGLIST.entryCap` tunável). **`services/world-entry`:** `admitOrEnqueue` (sob o teto + vaga → entra IMEDIATO; senão → fila) + `runAdmissionPass` (DIÁRIO, FIFO-com-skip, herda as vagas liberadas). **`services/scheduler`:** fia o passe no FIM do `processDay`. **Revisão adversarial** (workflow 3 dimensões): o núcleo voltou sólido; **2 MINOR corrigidos** — (1) o admitido HOJE herdava o resultado/**lesão** da rodada já publicada do NPC (uma partida que não jogou) → **fix: admissão no FIM do dia** (entra, processa amanhã); (2) o imediato **furava a FIFO** de quem já esperava a mesma posição → **fix: enfileira se contestado** — + **1 NIT** (comentário do lock) + **1 MAJOR/MINOR deferido** (teto TOCTOU concorrente — **não alcançável** no sistema atual [o passe é sequencial-seguro; não há rota HTTP]; o guard atômico fica com o card da rota). **437 testes** (429 preservados + 8 novos), typecheck/eslint/build/prettier verdes; **engine + os 4 goldens intocados** (`git diff` = 0; tudo é borda + a migration da fila). **Escopo deferido:** a **entrada de TIME (SPEC-035** — takeover + rebrand); o `markActive` real (HTTP/sessão); a **auditoria da viragem** (lê o `published_round` — cura o trade-off + o débito da SPEC-029); a Pirâmide Elástica; a calibração do `entryCap`. **(Mergeado em `main` — PR #37.)**
- **SPEC-036 — Camada de dados 0.2 · Fatia 5: Pirâmide Elástica (R13)** (FECHA a camada de dados 0.2 — o mundo passa a CRESCER): quando o andar de entrada enche de humanos (~70%), a virada **expande** (alarga a base até o teto 2× da largura do andar acima; quando satura, nasce um ANDAR NOVO embaixo), determinística por seed, na tx de rollover; e a **promoção passa a cruzar GRUPOS PARALELOS** (o mínimo correto — o playoff de acesso rico ficou pro card de produto 2.2). **Golden-safe por CONSTRUÇÃO** (o critério DURO): **(1)** a promoção de 1-grupo fica INTOCADA via **dispatch** no `advanceWorld` (todos os andares com 1 grupo → o `promotion.ts` v1 byte-idêntico; grupos paralelos → `promotion-multi.ts` novo); **(2)** a expansão é gated num 5º param `expand?` (default `false`, **ZERO PRNG consumido**, retorna a mesma referência — padrão `immuneIds` da SPEC-021); **(3)** o golden roda 100% NPC → 0% ocupação → nunca expande → **byte-idêntico**. **Engine:** `expansion.ts` (regra de crescimento + seeding NPC via `createClub`, índices globais CONTINUADOS = únicos), `promotion-multi.ts` (rank ACHATADO reusando `newMembers`/`assertConservation` + re-empacota em grupos de 20), `tierAbilityRange`/`boundaryK` com clamp (andar novo = banda de várzea; fronteira nova herda o último valor). **Borda:** `entryOccupancyRate` (% humanos em clubes da entrada) → `persistWorldTurnover` injeta `expand`. **Novo golden** `world-expansion.golden.json` (cadeia com `expand:true` forçado — prova a topologia + a promoção multi-grupo). **Revisão adversarial** (workflow 3 dimensões): golden-safety/determinismo E expansão/seam/OPs **LIMPAS**; **2 achados MINOR/NIT corrigidos** (overflow silencioso no re-pack se `promoteRelegate` for reconfig → guard fail-loud [config-gated, default nunca dispara]; sort lexicográfico além de 999 clubes → ordena por comprimento+lex = numérico, golden intocado). **453 testes** (437 + 16), typecheck/eslint (guardrail)/build/prettier verdes; **os 4 goldens byte-idênticos** (`git diff` = 0); **sem migration**. **(PR pendente; card em `spec` — aguardando o clique de aprovação da SPEC no board.)**
- **SPEC-028 — Página Coming Soon na Steam (Trilha GTM · G.3)** (a **primeira entrega da trilha de marketing** — a wishlist da Steam vira a lista de espera nativa, substituindo a landing própria D9/D10): **docs/GTM-only** (zero código/schema/API). Nasce a pasta **`docs/gtm/`** (convenção nova, aprovada) com a **copy fonte-de-verdade** (`store-copy.md` — nome Next Goat, short desc ≤300, "About This Game" em BBCode fundindo os ângulos ambiente+coop, tags/gêneros sem `Management`, aviso legal; PT-BR + EN stub), o **runbook do Steamworks** (`steamworks-runbook.md` — enrollment, taxa $100+recoup, specs de assets, review 3–5 dú + 2 semanas + hold 30 dias, wishlist tracking, Next Fest demo-obrigatória, festivais sem demo) e o **gate de demanda** (≥2.000 wishlists/90d gross adds + 1 festival). **Revisão adversarial** (workflow 3 lentes + verificação): 4 defeitos corrigidos — incl. um **"drift de cadência" FALSO** que a IA afirmou fiada no snapshot de instruções (o charter já é diário 7/7 desde a SPEC-011) → pego por `grep` no disco. Travas honradas (mundo fictício, zero aposta salvo negação, cadência diária, promessas verificáveis) — provadas por grep. **Go-live GATED** (não publica nesta entrega): capsule (G.1) + jurídico (G.2) + build de cliente p/ screenshots reais (pós-F0). **(PR pendente; aprovação/stages na UI H1VE.)**

**Convenções cravadas (ver `README.md`):**
- Layout: libs de domínio puras sob `packages/*` (docs falam `lib/world-engine` ⇒ `packages/world-engine`); borda impura só em `harness/`.
- Camadas (OP-17): libs puras = toda a lógica · orquestração fina · cliente só renderiza.
- Determinismo (money path): sem `Math.random`/`Date.now`/`new Date()`/`Intl`/transcendentais/relógio/entropia em `packages/*/src` (lint + golden cross-ambiente).
- i18n: sem texto de UI hardcoded; libs puras sem strings localizáveis.
- Runtime: Node ≥ 20.19.

**Próximo:** com a **identidade humana DENTRO DO MUNDO (SPEC-020)**, o **mundo virando sozinho + o humano sobrevivendo à virada (SPEC-021)**, a **carreira com FIM que vira começo (SPEC-022)**, a **retenção/escassez — congelamento de vaga (SPEC-023)**, a **economia — salário e estilo de vida (SPEC-024)**, a **agência das 18h — decisões de carreira (SPEC-025)**, as **lesões narrativas com arco (SPEC-026)**, as **duas barras Forma/Moral vivas (SPEC-027 — fatia A)**, **Forma/Moral APLICADA NA PARTIDA (SPEC-029 — fatia B)**, o **scheduler de produção (SPEC-030)** — o **tick diário idempotente** que faz o mundo jogar às 15h SOZINHO (publica+modula+paga+decai+decide+recupera+vira+regenera+expira) a **partida rica: ocorrência de lesão (SPEC-031)** — a partida finalmente **MACHUCA** (o `injureFromMatch` órfão da SPEC-026 dispara de um evento determinístico, **sem tocar o placar nem os goldens**) — o **cron/deploy + catch-up completo (SPEC-032)** (o mundo joga às 15h **SOZINHO** num relógio real, com catch-up de qualquer dia perdido + o worker em container + runbook) — as **transferências: o humano como ALVO do mercado (SPEC-033)** (o mundo assedia por heurística, aceitar te MOVE de clube na viragem) — e agora a **entrada imediata + waiting-list solo (SPEC-034)**: o solo entra **na hora** (mid-season — a investigação provou a guarda de gênese conservadora demais), com um teto que vira fila e um passe diário que herda as vagas liberadas. O arco "da várzea às lendas" + o ciclo de presença + os stakes materiais + a agência + a cor narrativa + o estado (Forma/Moral que MEXE no resultado) + o batimento diário de produção + a partida que gera história + o gatilho de produção com catch-up + a mobilidade de carreira + **a entrada com escassez** estão fechados em código. Frentes naturais: **(1) EXECUTAR o deploy** — subir a imagem no Railway/Render, semear os segredos e a âncora (**ação de OPS do founder**; o código está pronto, o runbook em `docs/ops/`); **(2) a entrada de TIME — SPEC-035** (takeover + rebrand: o quinteto ≥5 assume um clube e o rebranda pro nome/kit dele, herdando a campanha; decisões já travadas); **(3) a auditoria da viragem** (a viragem lê o `published_round` em vez de re-simular → cura o trade-off do mid-season + o débito de replay da SPEC-029; **card a criar**); **(4) a partida rica — fatia 2** (o timeline de gols; os eventos de ESCOLHA 3.2; a lesão AFETAR o placar); **(5) o mercado NPC rico — Transferências Fatia 2**; **(6) mid-season**; **(7) encaixe da Copa**; **(8) o paywall real** do Regen; **(9) a rota HTTP/auth** (+ o `markActive` real + o guard atômico do teto). Escolher sempre no **board vivo** (`h1ve start`; o board é a fila, **não** o roadmap, ritual SPEC-176). **⚠️ Gotcha:** `h1ve start <n>` é **posicional** na lista viva — reconfere o nome no retorno (para avançar o card ATUAL de `spec`→`dev`, a aprovação da SPEC já move sozinho; não rode `h1ve start <n>` de novo). Outras frentes: **Fatias 4-5 da 0.2** (Neon → Pirâmide Elástica — **cards no board**); **Trilha GTM — G.1** (**card no board**). Depois: 0.3 RNG/auditoria → 0.4 segurança.

---

*CLAUDE.md gerado pelo Nexus Flow (H1VE) na fundação do projeto. Documento vivo — leia antes, atualize depois.*
