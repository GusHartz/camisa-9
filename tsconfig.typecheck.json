{
  "//": "Gate de type-check (SPEC-001). Inclui os TESTES (que o build composite exclui de propósito p/ não emitir em dist). noEmit — nunca gera artefato. Cobre o furo: erro de tipo em teste reprova o gate `typecheck`.",
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "composite": false,
    "declaration": false,
    "declarationMap": false,
    "sourceMap": false,
    "types": ["node"]
  },
  "include": [
    "packages/*/src/**/*.ts",
    "harness/**/*.ts",
    "services/*/src/**/*.ts",
    "services/*/test/**/*.ts",
    "services/*/*.ts"
  ]
}
