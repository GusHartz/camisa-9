# Copie para `.env` (git-ignorado) e ajuste. Server-only (OP-02/OP-12) — nunca commite o .env real.
# Aponta para o Postgres local do docker-compose.yml (mesmo banco/credenciais).
# Se subir o compose com POSTGRES_PORT=5434 (porta 5432 ocupada), troque a porta aqui também.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/camisa9_dev
