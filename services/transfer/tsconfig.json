{
  "//": "Type-check da transferência (SPEC-033) — a costura do MOVE. Como o regen/world-entry, NÃO é composite e NÃO entra no `tsc -b`; borda IMPURA rodada via tsx/vitest; consome os stores + a lib pura pelos `paths` (→ src). O gate de tipos autoritativo é `tsconfig.typecheck.json` (raiz), que inclui services/*.",
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "resolveJsonModule": true
  },
  "include": ["src/**/*.ts", "test/**/*.ts"]
}
